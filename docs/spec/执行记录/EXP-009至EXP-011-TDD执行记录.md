# EXP-009 至 EXP-011 TDD 执行记录

- 目标行为：Voice 主文案不得保存“的3元”等残句，不得只替换记录变量后复用近期表达骨架；Planner 已选首候选时，通用文案不能绕过候选事实。
- 当前行为：生产案例中的残句和近似文案均被保存；近期原文只作为 Prompt 软提示，最终输出没有完整性、句式相似度或首候选硬门禁。
- 权威来源：`docs/spec/20-陪伴表达事实契约.md` 场景 `EXP-009`、`EXP-010`、`EXP-011`，以及 `EXP-004` 两位小数口语金额扩展。
- 基线提交：`origin/main@31ccefa`。
- 工作树：`D:\Business\count\.worktrees\陪伴文案完整性TDD`。
- 分支：`codex/陪伴文案完整性TDD`。
- 本轮范围：Voice 单次多候选协议、残句完整性、最终句式相似度、Planner 首候选覆盖、`10块48` 金额归一、Edge 测试与 CI 门禁。
- 非范围：历史记录回写、数据库迁移、客户端修句、在线模型评测、第二次 Voice 重试、生产 Edge 部署。
- 预计修改文件：表达事实 Spec、执行记录、Voice Prompt/出口、金额校验、Deno 测试和 Release Validation 测试列表。
- 基线测试结果：`signals_test.ts`、`context-packet_test.ts`、`prompts_test.ts` 共 49/49 通过。
- 红灯测试及失败原因：新增专项共 30 项中 25 项通过、5 项按预期失败；残句被视为完整、相似度恒为 0、单次响应只读取首句、选择器直接采用残句，`10块48` 被误拆为 `10.4` 与 `48`。
- 最小实现：Voice 一次响应提供首选句和最多两条备选；代码按完整性、近期三元表达骨架、数字/统计事实和 Planner 首候选覆盖依次选择。全部失败时使用确定性候选。口语金额解析支持紧邻的一至两位小数及明确角分单位，不放宽今日累计等统计口径。
- 绿灯结果：专项与关联 Deno 测试 55/55 通过；Edge 目录全量 91/91 通过；Planner 全量 183/183 通过；`deno check`、安全契约和 PWA 生产构建通过。
- PWA/iOS 差异：修复在 Edge 持久化前统一生效，两端不新增修句逻辑；本轮没有 Swift 代码，因此无需本地 iOS 编译结论。
- GitHub CI 结果：尚未推送，未运行。
- 未解决风险：字符三元组阈值需要继续通过真实样本观察误判率；在线模型是否稳定返回备选表达需要生产 Canary 验证，但缺失备选时保持向后兼容并走确定性兜底。
- 对应提交：尚未提交。
