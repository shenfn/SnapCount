# PWA-034 至 PWA-039 中转归档数据库契约执行记录

> 日期：2026-08-16
>
> 分支：`test/中转归档原子契约`
>
> 基线：`2430cf8`

## 目标与范围

- 用 PostgreSQL fixture 固定 expense、income、通用域归档的事务、幂等和权限行为。
- 对不同域幂等重试的伪引用风险建立失败用例和最小迁移修复。
- 不修改 PWA/iOS、Edge、生产数据或生产迁移历史。

## 实现

- 新增 `scripts/test-staging-archive-migration.sql`，覆盖成功归档、账户流水、金额拒绝、同域幂等、跨用户、anon、通用域和故意失败回滚。
- 新增 `20260816100000_archive_idempotency_domain_guard.sql`，保留既有归档实现，仅在幂等入口按真实目标校验域并返回真实引用。
- CI 在独立数据库中应用现有 finance fixture、当前权威迁移和新迁移；两条迁移均连续执行两次。

## 已验证

| 命令 | 结果 |
|---|---|
| `npm run governance:check` | 通过 |
| `npm run check:security-contracts` | 通过 |
| `npm run check:receipt-dedup` | 通过 |
| `npm run test:finance-occurred-at` | 通过 |
| `npm run build` | 通过；仅保留既有 bundle/eval 警告 |
| `node scripts/check-migration-versions.mjs` | 通过；新迁移登记为 pending |

## 未验证与风险

- 本机 Docker/PostgreSQL 未运行，新增 SQL fixture 尚未本地执行；必须以 GitHub Actions 的 PostgreSQL job 为准。
- 生产迁移 `20260808054219` 与仓库 `20260808120000` 的映射仍未修复，不执行 `migration repair`。
- 未执行生产迁移、部署或真实业务写入。

## 下一步

1. 提交并创建 PR，等待 PostgreSQL、治理、安全和构建门禁。
2. 若 PostgreSQL fixture 失败，只在本分支修正测试基建或最小迁移，不进入 PWA 接线。
3. 全绿并合并后，从最新 main 创建 `feature/PWA中转原子归档`。
