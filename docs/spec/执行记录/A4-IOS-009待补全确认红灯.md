# A4-IOS-009 iOS 待补全确认动作编排执行记录

> 状态：最小实现已接入，等待 macOS XCTest/门禁验证
>
> 日期：2026-08-22
>
> 基线：`17f023e`（A4-IOS-009 边界评估合并提交）

## 本次范围

本片只固定 iOS 待补全支出/收入确认这一条 canonical RPC 写动作。中转生命周期、手动财务保存、正式记录编辑/删除、记录详情、图片签名、表达反馈、截图还款和词汇规则继续冻结。

## 红灯产物

- 新增 `ios/SnapCountTests/PendingConfirmationUseCaseTests.swift`，登记 A4-IOS-009A-H：输入拒绝、同 pending 并发、expense/income 响应、失败保留、accepted/refresh 分层、独立副作用、reset stale 和非目标边界。
- 新增 `scripts/test-ios-pending-confirmation-boundary.mjs`，固定 Use Case 无 transport、Repository 结构化确认结果、AppState 兼容投影和 reset 接线。
- 新增 `npm run test:ios-pending-confirmation-boundary`。
- 新增待补全确认动作编排 Spec；阶段索引切换到 A4-IOS-009A-H 红灯。

## 红灯验证记录

- 初始运行源边界检查时，`PendingConfirmationUseCase.swift` 尚不存在，Repository 仍返回 `Void`，AppState 仍直接调用 transport，reset 也未接线；这些缺口按预期产生红灯。
- 当前红灯已由最小实现接入覆盖；Windows 不编译 Swift，正式行为验证以 macOS XCTest 为准。

## 最小实现进展

- `InboxRepository` 新增窄 `PendingConfirmationRepositoryProtocol`，将 RPC 响应映射为 `NativePendingConfirmationResult`，保留 `record_type`、正式记录 id/reference 和 `idempotent_retry`。
- 新增纯编排 `PendingConfirmationUseCase`，统一输入拒绝、同 pending 复用/冲突、session/generation stale、accepted/refresh 分层和 reset。
- `AppState` 保留 `confirmPendingRecord` 入口，只负责 busy、导航、消息、表达计划失效、词汇学习和账户/dashboard 刷新兼容投影。
- 既有 `InboxRepositoryProtocol` 测试 stub 已适配结构化返回；RPC 名称和九个参数未改变。

## 后续顺序

1. 运行专项 Node、macOS XCTest、PWA/Edge、治理和架构门禁；按业务失败、环境失败和测试契约错误分层处理。
2. 全部门禁通过后更新阶段索引和交接快照，并进入下一片只读评估。
3. 不执行生产迁移、部署、真实数据写入或 TestFlight。
