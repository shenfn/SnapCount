# EXP-004 TDD 执行记录

- 目标行为：`6块8` 等口语金额与代码金额 `6.8` 等价，且不能把无关数量拼成小数金额。
- 当前行为：裸数字提取器把 `6块8` 和 `6元8角` 拆成 `6`、`8`，中文“六块八”则完全未进入数字闭环。
- 权威来源：`docs/spec/20-陪伴表达事实契约.md` 场景 `EXP-004`。
- 基线提交：`origin/main@6ae2d5c`，叠加本分支已完成的 `EXP-001` 至 `EXP-003` 本地改动。
- 工作树：`D:\Business\count\.worktrees\事实与表达正确性TDD`。
- 分支：`codex/事实与表达正确性TDD`。
- 本轮范围：金额上下文中的有限口语语法、数字闭环与 Deno 回归测试。
- 非范围：OCR 金额识别、优惠/原价计算、所有中文数量词、模型文案重写、客户端和数据库迁移。
- 预计修改文件：表达事实 Spec、执行记录、`signals.ts`、`signals_test.ts`。
- 基线测试结果：`npx --yes deno test --no-lock supabase/functions/ingest-receipt/signals_test.ts`，22/22 通过。
- 红灯测试及失败原因：首轮 24 项中 23 项通过、1 项按预期失败：`6块8` 被提取为不受支持的 `6`、`8`；复核新增统计口径用例后，25 项中 24 项通过、1 项按预期失败：`近90天平均单笔6块8` 仍被旧统计解析器误读为 `6` 元。“6块商品、8份”反例始终保持通过。
- 最小实现：数字提取器先识别“元/块 + 紧邻一位角数”的完整跨度，将阿拉伯或中文元角表达归一为小数金额，再跳过跨度内部的裸数字；统计声明解析复用同一完整跨度及其原有 meaning/scope，不相邻的商品数量保持独立。
- 绿灯结果：`npx --yes deno test --no-lock supabase/functions/ingest-receipt/signals_test.ts`，25/25 通过。
- 本分支最终回归：Edge 相关 Deno 测试 85/85、Planner 全量 176/176、PWA 生产构建和 `git diff --check` 均通过。
- PWA/iOS 差异：金额校验在 Edge 持久化前统一执行，客户端无需重复解析。
- GitHub CI 结果：未运行。
- 未解决风险：本轮只接受高置信的元角口语结构，更复杂的“六块八分五”等表达仍按旧规则处理。
- 对应提交：随本次 PR 提交。
