# A4-IOS-011 记录详情图片加载红灯执行记录

> 日期：2026-08-22
>
> 分支：`test/A4-IOS-011记录详情图片红灯`
>
> 基线：`e38729e`（A4-IOS-011 Spec 合并提交）

## 红灯目标

- 固定 A4-IOS-011A-G：无图片路径、已有 URL、并发 identity、事实透传、失败分层、reset/context stale 和非目标边界。
- 证明当前基线仍由 AppState 私有方法直接调用 `RecordRepository.hydrateDetailImage`，尚无独立 Use Case、窄协议或专项 stale/并发契约。

## 本轮新增

- `ios/SnapCountTests/RecordDetailImageUseCaseTests.swift`：引用尚未存在的 `RecordDetailImageUseCase`、`RecordDetailImageUserContext` 和窄 Repository 协议，预期当前基线编译失败。
- `scripts/test-ios-record-detail-image-boundary.mjs`：检查未来 Use Case 的纯 Swift、并发/stale 责任与 AppState 兼容投影边界；当前基线因 Use Case 文件不存在而失败。
- `package.json`：登记 `npm run test:ios-record-detail-image-boundary`。

## 预期失败分类

- 业务红灯：缺少目标 Use Case、结果契约和窄 Repository 协议。
- 非业务失败：Windows 无法运行 Swift/XCTest；macOS CI 仅用于记录红灯编译证据，不把环境缺失当作业务结论。

## 红灯证据

- `npm run test:ios-record-detail-image-boundary`：基线 5 项中 3 项通过，2 项因目标 Use Case 文件不存在而失败；失败属于预期业务红灯。
- PR #146 的 macOS Build 明确报 `RecordDetailImageUseCase`、`RecordDetailImageUserContext` 和 `NativeRecordDetailImageRepositoryProtocol` 不存在；PWA/Edge、治理、Vercel 和 Cloudflare 门禁通过。

## 最小实现进展

- 已在实现分支加入 `RecordDetailImageUseCase`、`NativeRecordDetailImageRepositoryProtocol`、AppState hydration 投影和 reset 接线。
- Use Case 负责 image path/URL 前置判断、session/context stale、同 identity Task 复用、失败分类和 reset；AppState 保留详情缓存、表达反馈字段合并和当前详情投影。
- 实现 PR 的 macOS XCTest、iOS Build、治理、架构、PWA/Edge 回归通过后，才更新本片为已完成。

## 下一步

1. 推送实现分支并运行专项 XCTest、源边界和 GitHub macOS Build。
2. 通过后更新 Spec、阶段索引和交接快照，记录失败/成功分层。
3. 实现阶段不得扩大到详情读取、表达计划、图片签名算法、数据库、Edge、PWA、账户或视觉。
