# A2 表达核心阶段收口执行记录

> 日期：2026-08-15
>
> 基线：`3390f9e`（PR #63 合并提交）

## 阶段范围

- CORE-001 至 CORE-011 的生产共享纯函数迁移、Lab wrapper、双运行时 fixture 和架构 ratchet。
- 不包含 PWA、iOS、Edge 编排拆分、数据库迁移、生产部署或 worktree 清理。

## 完成证据

- CORE-001 至 CORE-010：已合并并通过各自 Node/Deno、Planner、Edge 和治理门禁。
- CORE-011：文档 PR #62 合并；实现 PR #63 合并，提交 `df5703d`，合并提交 `3390f9e`。
- PR #63 Release Validation：PWA build、Node/Deno expression-core、完整 Planner 185/185、Edge `deno check`、迁移、安全和治理门禁全部通过。
- 架构依赖：`production_tools_imports: 0`；共享核心边界检查通过。
- 生产与 Lab 共用 `_shared/expression-core`，fallback 路径保留；未修改 SQL 历史 fixture。

## 严格接续演练

- 首先尝试启动独立 `codex.exe`，Windows App 安装目录返回“拒绝访问”；这是演练工具环境失败，仓库未变化。
- 随后启动 `fork_turns: none` 的全新 Agent，只提供 worktree 路径、任务编号 `GOV-003` 和只读约束，不提供聊天历史或文件名。
- 新 Agent 正确恢复 A2 当前任务、`3390f9e` 基线、冻结范围、CORE-001 至 CORE-011 范围、PR #63 CI 证据、`production_tools_imports: 0` 和 A3 下一步。
- 新 Agent 未修改文件，并额外发现表达核心总 Spec 与 CORE-011 Spec 的状态陈旧；首轮因此只判定恢复能力通过，不判定阶段门禁通过。
- 两处状态已在本收口切片修正；第二轮零历史复验再次恢复成功，但发现 CORE-003 至 CORE-010、A1 历史报告和若干执行记录/交接仍有陈旧状态。
- 第二轮未修改文件，A2 继续保持进行中；上述漂移已进入本收口切片修复，最终验收需运行第三轮零历史复验。
- 第三轮零历史复验恢复成功，但发现阶段索引仍使用不存在的“Phase 3 → Phase 4”，与总计划的 A2 → A3 路线冲突；第三轮因此不通过且未修改文件。
- 阶段索引已统一为 A2 → A3；最终验收需运行第四轮零历史复验。
- 第四轮零历史复验恢复成功，但发现 GOV-003 缺少当前交接直链，且 CORE-011 两份旧交接未标注历史；第四轮因此不通过且未修改文件。
- 当前索引已直接指向 GOV-003 收口快照，两份 CORE-011 旧交接已标记为历史；在该时点仍需运行第五轮零历史复验。
- 第五轮只读终验确认当前阶段、冻结范围、CORE-001 至 CORE-011 完成证据、A2 → A3 路线、GOV-003 直接交接指针和历史快照隔离均一致；未发现阻断收口的现行文档冲突。
- 第五轮未修改文件；本地治理、架构、表达核心边界、Node 核心测试和 `git diff --check` 均通过。

## 阶段门禁

- A2 的实现、CI 和第五轮恢复终验均已验证；前四轮发现的文档漂移已在本切片修正。
- PR #64 首轮治理、综合发布校验和 iOS Gate 已全部通过；A2 阶段门禁完成，下一阶段先做 A3 PWA 业务边界评估，不直接开始大规模端侧重构。
- `governance:arch` 的非阻断警告仍含旧阶段提示，登记为 A3 边界评估开始前的治理文案清理项；本 A2 文档切片不修改脚本。

## 冻结范围

- 根工作区和其他 worktree 的所有 WIP。
- PWA/iOS 页面、数据库迁移、生产配置、部署和 TestFlight。

## 下一步

1. 推送 A2 完成状态提交并等待 PR #64 再次通过门禁。
2. 合并 PR #64。
3. 从最新 main 建立 A3 PWA 边界评估 Spec 和只读审计。
