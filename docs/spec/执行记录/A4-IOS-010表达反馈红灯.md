# A4-IOS-010 表达反馈动作编排执行记录

> 日期：2026-08-22
>
> 分支：`test/A4-IOS-010表达反馈红灯`
>
> 基线：`d8a89a8`（A4-IOS-010 边界评估合并提交）

## 红灯与实现目标

- 固定 A4-IOS-010A-G：输入拒绝、并发复用、事实透传、失败分层、reset stale、副作用边界和页面入口边界。
- 证明当前 AppState 直调用 `RecordRepository.submitFeedback`，缺少独立 Use Case、窄协议和 session/generation stale 隔离。

## 红灯提交

- 新增 `docs/spec/模块/iOS业务边界/表达反馈动作编排规格说明.md`。
- 新增 `ios/SnapCountTests/FeedbackSubmissionUseCaseTests.swift`，引用尚未存在的 `FeedbackSubmissionUseCase` 与窄协议，预期当前基线编译失败。
- 新增 `scripts/test-ios-feedback-submission-boundary.mjs` 并登记 npm script，预期当前基线因 AppState 尚未接入 Use Case 而失败。
- 更新规格文档索引和阶段索引。

## 最小实现提交

- `1a7efd2`：加入 `FeedbackSubmissionUseCase`、`NativeRecordFeedbackRepositoryProtocol`、AppState 兼容投影和 reset 接线。
- Use Case 负责 session/generation stale、命令 identity、同命令 Task 复用、失败分类；Edge action 和反馈参数保持不变。
- 未修改数据库、Edge、PWA、Planner、正式记录保存/删除、图片签名或页面视觉。

## 验证

- `npm run test:ios-feedback-submission-boundary`：实现后通过。
- 红灯阶段 GitHub Run `32548782411`：macOS 明确报缺少 `FeedbackSubmissionUseCase`、输入模型和窄 Repository 协议，属于预期业务红灯。
- 实现阶段 PR #142 全部门禁通过：macOS Swift 编译/XCTest 5m16s、iOS Build Gate、Release/Governance/PWA/Edge、Vercel、Cloudflare 全绿。
- Windows 不运行 Swift/XCTest；macOS CI 是 iOS 编译与测试权威。
- `git diff --check`：通过。
- `npm run governance:check`、`npm run governance:arch`：通过；架构检查仅有既有人工清单警告。

## 未验证与剩余风险

- 正式记录详情、保存、删除和图片签名仍未收拢，继续由后续独立评估决定。
- 未执行生产部署、数据库迁移、真实数据写入或 TestFlight。

## 下一步

1. 从最新主线只读评估下一片 iOS 业务边界；未完成评估前不修改下一片业务代码。
2. 继续遵循评估 → Spec → 红灯 → 最小实现 → macOS 验证 → 文档收口顺序。
