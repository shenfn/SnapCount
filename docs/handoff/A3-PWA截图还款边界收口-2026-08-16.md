# A3 PWA 截图还款边界收口交接快照

> 任务：PWA-067A 至 PWA-067G 实现收口
>
> 状态：已完成
>
> 基线：`8fc67d2`（PR #105 合并提交）
>
> 收口分支：`docs/PWA截图还款边界收口`

## 已完成

- PR #105 已合并，merge commit 为 `8fc67d2e0b5b2c5d0ccb4ccf7c20ed327db6e49b`。
- 候选评分已收敛为账户域纯函数；Account Repository 独占截图还款 transport，独立 Screenshot Repayment Feature 负责并发、冲突、stale 与 accepted/refresh 分层。
- 数据库根据金额推导 `paid/partial_paid/minimum_paid`，并在同一事务更新 payment、entry、balance、cycle 与 staging。
- 已处理中转目标显式写为 `repayment_cycle/wallet`；PagePending 只在 accepted 后推进，目标缺失有稳定失败出口。
- PWA-067A 至 PWA-067G 的 Spec、ADR-028、执行记录、测试映射和完成定义均已有仓库内证据。

## 验证证据

- 本地专项 30 项、手动还款 18 项、中转读取 11 项、账户读取 20 项、账户管理 19 项、财务保存 16 项通过。
- `npm run test:screenshot-repayment-contract`、安全契约、构建、治理、架构与 diff 检查通过；构建只有既有 `vconsole eval` 和 bundle size 警告。
- PR #105 首轮 Release Validation run `31943619573` 的 PostgreSQL 17 transaction step 成功。
- 补证提交 `1e6d69f` 的第二轮 Release Validation run `31943782706` 再次成功；两轮全部适用门禁通过。
- PostgreSQL fixture 连续执行 migration 两次，并验证全额、普通部分、最低还款、余额方向、source、幂等、冲突、discarded、跨用户、anon 权限和保守回填。

## 未验证与风险

- 未执行生产查询、生产 migration、部署、真实数据写入或 TestFlight。
- iOS 仍保留旧截图还款客户端状态参数；数据库已忽略该状态权威，跨端候选与 Feature State 对齐留给 A4。
- wallet 快照尚未提升为正式 `data_records` 证据并原子关联 cycle/payment；该范围属于 PWA-068。

## 冻结范围

- 根工作区和其他 worktree 的 WIP。
- PWA-069 流水 helper、iOS、Edge、Planner、生产配置、生产 migration 与部署。

## 下一步

1. 从最新 `origin/main@8fc67d2` 建立 `docs/PWA钱包快照边界评估`。
2. 只读评估 PWA-068：`createAccountFromWalletSnapshot`、`linkWalletSnapshotToAccount`、cycle、余额校准、repair、`evidence_record_id`、幂等和部分失败出口。
3. 评估结论合并前不建立实现 Spec、不修改业务代码；PWA-069 与 A4 继续冻结。
