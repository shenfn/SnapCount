# A3 PWA 账户管理边界实现交接快照

> 任务：PWA-066A 至 PWA-066F
>
> 状态：本地实现与回归已完成，待远程 PostgreSQL 17 行为验证
>
> 基线：`d42fcba`
>
> 分支：`feature/PWA账户管理边界实现`

## 上游已完成

- PWA-066 评估 PR #100 和收口 PR #101 已合并。
- ADR-027 已锁定数据库原子权威、旧客户端兼容、软归档和 iOS A4 后续复用。

## 当前目标

- 完成 PWA-066A 至 PWA-066F 的 PostgreSQL 契约、Repository、Management Feature、Store/Page 接线和专项测试。
- 保证默认互斥、归档恢复、会话 stale 与 accepted/refresh 分层可执行验证。

## 冻结范围

- 根工作区与其他 worktree 的全部 WIP。
- wallet 快照创建/关联/repair、截图还款、流水 helper、单账户 delete、iOS、Edge、Planner、生产配置和部署。

## 已完成

- 已建立正式 Spec 和初始执行记录。
- 已新增数据库 fixture/assertion、静态契约和 CI job 接线。
- `npm run test:account-management-contract` 因目标 migration 缺失而以 `ENOENT` 失败，PWA-066A/PWA-066B 有效红灯成立。
- 已实现数据库 migration、Account Repository、Account Management Feature、Store 接线、专用归档/恢复动作和已归档列表。
- 账户管理专项 18 项及相关读取、还款、补绑、财务保存回归均通过；Vite build、治理和架构检查通过。

## 下一步

1. 提交、推送并创建 PR。
2. 由 GitHub PostgreSQL 17 job 验证 migration 双次执行和行为 assertions。
3. 全部门禁通过后合并并建立 PWA-066 实现收口；A4 iOS 另开独立任务。

## 未验证与风险

- 本机没有可用 PostgreSQL/Docker 行为环境，SQL 行为尚未本地执行。
- 生产存量多默认/归档默认现状未知。
- iOS 仍使用旧 REST 写入，必须等待远程 fixture 证明 trigger 兼容后才能合并。

## 发布边界

- 未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
