# A3 PWA 账户还款边界评估交接快照

> 任务：PWA-064
>
> 状态：已完成，PR #93 已合并
>
> 基线：`85882db`
>
> 计划分支：`docs/PWA账户还款边界评估`

## 上游已完成

- PWA-057 至 PWA-062 已收拢 manual/edit 财务保存 transport 与编排边界。
- PWA-063A 至 PWA-063F 已建立独立 Account Binding Feature，并由 PR #91 全门禁通过后合并，merge commit `35dae06`。
- PWA-063 收口 PR #92 已通过远程门禁并合并，merge commit `85882db`。
- Record Repository 已持有 canonical 财务保存 DTO；Store 不再直接组装账户补绑保存 RPC。

## 本轮目标

- 只读盘点账户 CRUD、账户流水、负债还款、还款周期、wallet 快照账户创建/关联和补偿 RPC 的调用链。
- 区分账户资料管理、还款业务编排、流水补偿和 wallet 快照关联，避免一次性实现多个不同职责。
- 定义后续最小实现切片的范围、非目标、场景编号、数据库契约和测试层，不直接修改业务代码。

## 冻结范围

- 根工作区与其他 worktree 的全部 WIP。
- 数据库 RPC、迁移、账户余额方向、推荐算法、默认账户、manual/edit、staging、待补全、删除、图片签名、iOS、Edge、Planner、生产配置和部署。
- 不执行生产查询、真实数据写入、迁移、部署或 TestFlight。

## 已完成

- 已盘点账户 CRUD、详情读取、还款周期、手动确认/撤销、截图还款、wallet 快照关联和流水补偿调用链。
- 已确认 `set_repayment_cycle_paid_amount`、`revoke_liability_payment` 和 `confirm_staging_repayment` 是账户、流水、payment 与 cycle 的服务端事务权威。
- 已确认当前 CI 没有覆盖还款确认、重新确认、撤销和跨用户拒绝的 PostgreSQL 行为测试。
- 已确认 PWA 手动还款缺少 generation、用户切换 stale 和 accepted/refresh 分层；iOS 已有 Account Repository 形状，但 AppState 编排不能作为这些语义的完成证据。
- 已将后续工作拆为五片，首片只处理数据库还款特征契约和 PWA 手动确认/撤销 Feature。

## 下一步

1. 已从 PR #93 合并后的 main 建立 `feature/PWA账户还款边界实现` worktree。
2. 当前任务见 `docs/handoff/A3-PWA账户还款边界实现-2026-08-16.md`。

## 验证摘要

- `test:wallet-snapshot-amount` 通过。
- `test:finance-save` 16 项、`test:account-binding` 11 项、`test:record-read` 14 项通过。
- `governance:check`、`governance:arch` 和 PWA build 通过；构建仅有既有 `eval` 与 bundle size 警告。
- 未连接真实服务；未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
