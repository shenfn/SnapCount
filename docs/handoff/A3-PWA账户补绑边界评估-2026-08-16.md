# A3 PWA 账户补绑边界评估交接快照

> 任务：PWA-063
>
> 状态：已完成，PR #90 已合并
>
> 基线：`564bd20`
>
> 计划分支：`docs/PWA账户补绑边界评估`

## 上游已完成

- PWA-057 财务保存 transport 边界评估已由 PR #87 合并。
- PWA-058 至 PWA-062 manual/edit 财务保存边界已由 PR #88 合并，merge commit `34c0774`。
- `recordRepository` 已持有两个 canonical 保存 RPC；Finance Save Feature 已持有并发、generation、用户切换和刷新失败分层。
- PR #88 第二轮治理、iOS 检测、预览和 PWA/Edge/迁移/Shadow 全部门禁通过。

## 本轮目标

- 只读核对 `bindRecordToAccount`、推荐补绑和批量补绑的调用链、返回事实、刷新编排与失败出口。
- 确认两处重复 `p_occurred_at` 键的实际影响，并决定应由 Repository 复用还是由独立 Account Binding Feature 编排。
- 定义 PWA-063 后续实现的范围、非目标、场景编号和测试层，不直接修改业务代码。

## 已完成

- 已核对单笔推荐补绑、手动补绑、批量预览与批量执行调用链。
- 已确认两个 canonical RPC 和 Record Repository 可复用，数据库事务规则无需修改。
- 已确认重复 `p_occurred_at` 当前由后一个同值键覆盖，但属于必须由 transport 收拢消除的静默缺陷。
- 已确认旧批量循环缺少用户/generation 隔离，且当前布尔结果无法表达部分成功与刷新失败。
- 已产出 `账户补绑边界评估.md`、ADR-024 和 PWA-063A 至 PWA-063F 后续场景。

## 冻结范围

- 根工作区与其他 worktree 的全部 WIP。
- 数据库保存 RPC、账户流水、余额方向、迁移、manual/edit、staging、待补全、wallet、删除、图片签名、iOS、Edge、Planner、生产配置和部署。
- 不执行生产查询、真实数据写入、迁移、部署或 TestFlight。

## 下一步

1. PR #90 已合并为 `30aed05`。
2. 实现任务见 `docs/handoff/A3-PWA账户补绑边界实现-2026-08-16.md`。

## 验证摘要

- `test:finance-save` 16 项、`test:record-read` 14 项、`test:finance-occurred-at` 和 `test:finance-options` 通过。
- `governance:check`、`governance:arch` 通过；架构检查只有既有人工清单警告。
- PWA build 通过，仅有既有 `eval` 与 bundle size 警告。
- 未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
