# A3 PWA 账户管理边界收口交接快照

> 任务：PWA-066 收口
>
> 状态：已完成，PR #100 已合并（`e0ef559`）
>
> 基线：`e0ef559`（PWA-066 评估 PR #100 合并提交）
>
> 分支：`docs/PWA账户管理边界收口`

## 已完成

- PR #100 已通过综合 Release Validation、治理、iOS gate、Cloudflare Pages 和 Vercel 预览检查并合并，merge commit `e0ef559`。
- ADR-027 已接受：默认互斥、归档非默认和跨端兼容由数据库 trigger、约束与 canonical RPC 负责。
- PWA-066A 至 PWA-066F 已覆盖数据库兼容契约、Repository、Feature、Store/Page、归档恢复和类型迁移边界。
- 当前主线仍未修改业务代码或数据库；评估证据与实现计划已持久化。

## 当前冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- PWA-066A 至 PWA-066F 只允许账户管理正式 Spec、数据库 migration/fixture、Account Repository/Management Feature、Store/Page 兼容接线及相关测试。
- wallet 快照创建/关联/repair、截图还款、流水 helper、单账户物理删除、iOS AppState、Edge、Planner、生产配置和部署继续冻结。

## 下一步

1. 从收口 PR 合并后的 main 建 `feature/PWA账户管理边界实现` 独立 worktree。
2. 先提交 PWA-066A 至 PWA-066F 正式 Spec 和测试映射。
3. 先写 PostgreSQL migration fixture，证明旧客户端直写兼容、默认互斥、归档/恢复和跨用户边界；确认有效红灯后做最小数据库实现。
4. 数据库契约通过后再接 Repository、Feature、Store/Page，并运行账户读取、还款、补绑、财务保存、构建与治理回归。

## 未验证与剩余风险

- 生产数据中是否已有多默认、归档默认或跨家族历史账户未知；本轮未连接生产服务。
- canonical RPC、trigger、约束、迁移 fixture 和 PWA 接线尚未实现。
- iOS 尚未接 canonical RPC；实现必须先证明存量直写兼容。
- 已登录浏览器的归档恢复、详情同步和失败提示尚未验证。

## 验证摘要

- 本地 `test:account-read` 17 项、`test:repayment` 15 项、`test:account-binding` 11 项通过。
- Vite build、治理和架构检查通过，仅既有警告。
- PR #100 远程全部适用门禁通过；未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
