# LOCAL-002 消费记录本地优先交接

> 更新时间：2026-08-23

## 当前状态

- 阶段：L1 / LOCAL-002。
- worktree：`D:\Business\count\.worktrees\LOCAL-002-DB2编辑删除红灯`。
- 分支：`test/LOCAL-002-DB2编辑删除红灯`。
- 基线：`origin/main@6c01100`。
- 当前批次：LOCAL-002-DB1 已通过 PR #152 合并；DB2 已取得有效红灯，最小实现正在 PR #153 验证。

## 已完成

- LOCAL-SPIKE-001 已通过 PR #151 合并，ADR-037 选择 GRDB/SQLite。
- 复核现有 AppState、RecordRepository、NativeDataService、表单草稿、账户读取模型和 Dashboard JSON 快照。
- 确认当前消费保存必须登录并调用 Supabase RPC，不能直接在 AppState 增加本地/云端分支。
- 固定 DB1 的 profile、账户、消费、流水、Outbox、重启和余额派生边界。
- Run `32617323551` 取得目标类型缺失的有效红灯。
- 增加 production GRDB adapter，Run `32617626804` 的 5 个专项测试和完整 223 个 XCTest 全部通过。
- AppState、页面和现有远端 Repository 仍未接线。
- DB2 已裁定：`deleted_at` 业务行作为 tombstone；命令使用 `expectedVersion`；金额或账户变化采用旧流水冲销加替代流水。
- DB2 红灯 Run `32618481883`：App build 成功，测试 target 因 `LocalExpenseUpdate` 缺失失败，其他适用门禁通过。

## 冻结范围

- 根工作区和其他 worktree WIP。
- PWA、Edge、Supabase、生产数据库和发布配置。
- AppState、页面、登录、现有远端 Repository 接线。
- 导入导出、同步发送、AI 和其他数据域。

## 恢复入口

1. `AGENTS.md`
2. `docs/spec/03-阶段与任务索引.md`
3. `docs/spec/模块/iOS本地优先/消费记录本地优先边界评估.md`
4. `docs/spec/模块/iOS本地优先/消费记录垂直切片规格说明.md`
5. `docs/spec/执行记录/LOCAL-002数据库首批红灯.md`
6. `docs/spec/执行记录/LOCAL-002-DB2编辑删除红灯.md`
7. `docs/decisions/ADR-037-iOS本地业务数据库选择GRDB.md`

## 下一步

等待 PR #153 的最小实现 macOS CI；通过后写回完整 XCTest 数量和 merge commit，再进入 LOCAL-002-PORT 导出导入规格。不要在 DB2 顺手接 AppState 或页面。
