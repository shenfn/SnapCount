# A2 CORE-007 比较候选评估交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心比较候选`

## 已完成

- 只读审查了 comparison 的 419 行实现、两个导出、12 个共享 helper、6 个直接测试、Edge 调用点和完整 Planner 消费方。
- 结论是保持一个生产模块、两个测试契约组；当前不抽取公共 primitives。
- 已建立 ADR-009 和 CORE-007 Spec，场景编号为 CORE-040 至 CORE-048。
- 已建立共享 Node/Deno fixture 和断言器；目标生产文件缺失时产生有效 `ERR_MODULE_NOT_FOUND` 红灯。
- 最小实现已完成：生产核心、Lab wrapper、Edge 导入、Node/Deno workflow 和 ratchet 均已更新。
- 本地 Node 双契约、wrapper、旧 6 项回归、前六片核心回归、边界和治理检查均通过；生产 `tools/` 依赖降为 5 项。
- GitHub PR #54 的 Node/Deno、Edge、完整 Planner、治理、PWA 和迁移门禁均已通过，并已合并为 `68434cb`。

## 当前边界

- 下一实现分支只迁移 comparison 权威模块，不修改公式或顺手迁移其他候选。
- 根工作区 WIP 和其他 worktree 继续冻结，不做清理或归档。

## 下一步

1. 只读评估 cross-record 的输入契约、纯函数边界和独立测试覆盖。
2. 若边界成立，先建立独立 Spec、fixture 和红灯，再实现迁移。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
