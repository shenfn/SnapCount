# A2 表达核心模块规格交接快照

> 任务：CLEAN-001/A2、CORE-001
>
> 日期：2026-08-15
>
> 分支：`docs/表达核心模块规格`
>
> 基线：`6cc043d`

## 已完成

- 从 A1 提交建立独立 A2 worktree，未修改根工作区或其他 worktree。
- 编写 `docs/spec/模块/表达核心/规格说明.md`，明确第一切片范围、非目标、输入输出、场景编号、双运行时约束、迁移顺序和完成定义。
- 写入 ADR-003：生产权威位于 `_shared/expression-core`；Planner Lab 和 fallback 双路径保留；事实规则只实现一份。
- 将模块规格和 ADR 接入规格索引，阶段索引切换为 A2。

## 当前边界

- 只有规格和决策文档，尚未创建 `_shared/expression-core` 代码。
- 尚未建立共享 fixture，尚未写入会失败的 Node/Deno 特征测试。
- 不修改 `expression-shadow-planner.ts`，不删除 `tools/ai-validation`，不改变生产行为。

## 未验证

- A2 特征测试尚未运行；这是下一步工作，不应把“规格通过治理检查”当成实现完成。
- Deno 双运行时环境仍未验证，当前 Windows 没有 `deno` 命令。
- 未执行 Edge 部署、生产迁移或线上回归。

## 下一步

1. 按 CORE-001 至 CORE-006 建立 `supabase/functions/_shared/contracts/表达核心-v0.1.json` 共享 fixture。
2. 先写 Node/Deno 红灯测试，固定结构化不变量和失败出口。
3. 再实现 `_shared/expression-core` 最小纯函数，并让双运行时测试转绿。
4. 测试通过后才评估 Planner Lab adapter 和 Edge 第一切片导入替换。
