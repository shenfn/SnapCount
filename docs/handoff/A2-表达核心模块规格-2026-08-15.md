# A2 表达核心模块规格交接快照

> 任务：CLEAN-001/A2、CORE-001
>
> 日期：2026-08-15
>
> 分支：`docs/表达核心模块规格`
>
> 基线：`6cc043d`

## 已完成

- 从 A1 提交建立独立 A2 worktree，未修改根工作区或其他 worktree。
- 编写 `docs/spec/模块/表达核心/规格说明.md`，明确第一切片范围、非目标、输入输出、场景编号、双运行时约束、迁移顺序和完成定义。
- 写入 ADR-003：生产权威位于 `_shared/expression-core`；Planner Lab 和 fallback 双路径保留；事实规则只实现一份。
- 将模块规格和 ADR 接入规格索引，阶段索引切换为 A2。
- 建立共享 fixture、Node/Deno runner 和共用断言 helper；先记录缺少核心模块的 Node 红灯，再实现最小纯函数。
- 实现 `parseFiniteNumber`、`roundMoney`、`normalizeEntityText` 和 `buildExpenseFactContract`，Node 特征测试转绿。
- 增加核心纯函数边界检查，并在 release-validation workflow 中加入条件化 Node/Deno 双运行时步骤。

## 当前边界

- `_shared/expression-core` 第一切片已接入 `expression-shadow-planner.ts` 的数值解析和支出事实契约；其余 Planner 规则仍保留 A1 基线。
- Node 特征测试和边界检查已通过；Deno runner 需要 CI 或可用 Deno 环境验证。
- Planner Lab 纯 Node 回归在排除 `esbuild` 环境测试后 103/103 通过；全量的 5 个失败均为依赖缺失。
- 不删除 `tools/ai-validation`，不改变 Edge 生产行为，不修改 PWA/iOS。

## 未验证

- Deno 双运行时环境仍未验证，当前 Windows 没有 `deno` 命令。
- GitHub Actions 的 Deno 双运行时尚未取得结果。
- Planner Lab adapter 已接入对应纯函数模块；完整 fallback 对比回归尚未完成。
- 未执行 Edge 部署、生产迁移或线上回归。

## 下一步

1. 在可用 Deno 环境运行 `deno_test.mjs`，确认与 Node 使用同一 fixture 得到相同结果。
2. 为 Edge 第一切片增加对比 fixture，确认 fallback 行为不变。
3. 在完整 Deno 验证后再迁移下一组候选规则。
