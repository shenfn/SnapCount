# A4-IOS-011 记录详情图片边界评估交接快照

> 日期：2026-08-22
>
> 分支：`docs/A4-IOS-011业务边界评估`
>
> 基线：`origin/main@e865d49`

## 已完成

- 从最新主线只读检查 `RecordRepository`、`NativeDataService`、`SupabaseImageURLProvider` 和 AppState 详情加载流程。
- 比较完整详情读取、图片 hydration、详情+表达计划三个候选。
- 确认首选 A4-IOS-011 为“记录详情图片签名与 hydration 动作编排”；完整详情读取和表达计划继续冻结。
- 新增 `docs/spec/模块/iOS业务边界/记录详情图片加载边界评估.md`，未修改业务代码。

## 验证结果

- 工作区从 `origin/main@e865d49` 建立，初始 clean。
- 已确认现有 `hydrateDetailImage` transport 和 AppState stale 检查可以作为窄边界迁移基础。
- `git diff --check`、治理和架构检查将在提交前运行。

## 未验证与剩余风险

- Windows 不运行 Swift/XCTest；本片尚未进入实现。
- 图片签名服务仍使用现有 bucket、缓存寿命和重试策略，评估不授权修改这些契约。
- 完整详情读取、表达计划、正式记录保存/删除仍需独立边界，不得在 A4-IOS-011 顺带迁移。

## 下一步

1. 合并本评估文档 PR。
2. 从最新主线建立 A4-IOS-011 Spec 和红灯 worktree。
3. 先固定图片成功、失败、无路径、并发和 stale，再进入最小实现。
