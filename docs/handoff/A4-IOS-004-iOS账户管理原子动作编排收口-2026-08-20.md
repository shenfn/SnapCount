# A4-IOS-004 iOS 账户管理原子动作编排收口

更新时间：2026-08-20

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-004 iOS 账户管理原子动作编排。
- 实现 PR：#124，merge commit `b929f2c`。
- 评估基线：`d5dc62f`，PR #123 合并提交。
- 收口 worktree：`D:\Business\count\.worktrees\A4-IOS-004收口`。
- 当前 `origin/main`：`b929f2c`。

## 已完成

1. 新增纯 Swift `AccountManagementActionUseCase`，不依赖 SwiftUI、HTTP、Supabase client 或页面。
2. 新增窄协议 `AccountManagementRepositoryProtocol`，不扩张账户读取/还款 Repository 的测试和职责边界。
3. `AccountRepository.saveAccount` 调用 `save_account`；`setAccountArchived` 调用 `set_account_archived`，移除账户管理旧 REST 多步写和 `unsetOtherDefaults`。
4. AppState 保留 `saveAccount`、`setAccountArchived`、busy、message 和页面公开入口；accepted canonical account 先收敛列表/详情，再独立刷新。
5. 固定同命令 Task 复用、同账户跨命令冲突、reset/user stale、accepted/refresh failure 和稳定失败出口。
6. 新增 A4-IOS-004A-H XCTest、Repository transport XCTest 和 `test:ios-account-management-action-boundary` 源边界检查。

## 验证证据

- macOS workflow `32382431565`：Build SwiftUI app（完整 XCTest）通过；iOS Build Gate 通过。
- PR #124 的 PWA/Edge/migrations/shadow、Governance、Cloudflare Pages 和 Vercel 全部通过。
- `npm run test:ios-account-management-action-boundary`：5/5 通过。
- `npm run test:account-management`：19 项通过。
- `npm run test:account-read`：20 项通过。
- `npm run test:repayment`：18 项通过。
- `npm run governance:check`、`npm run governance:arch`、`npm run build`：通过；仅有既有人工清单、`eval` 和 bundle size 警告。

## 未验证与剩余风险

- Windows 无 Swift/Xcode；iOS 权威验证来自 GitHub macOS runner。
- 未执行生产迁移、生产部署、真实账户写入或 TestFlight。
- 账户详情读取前的 `ensureRepaymentCycles` 写副作用仍未拆分。
- 截图还款候选读取与确认仍是独立跨 staging/cycle/account 切片。
- 根工作区和其他 worktree 的 WIP 未触碰，禁止以本收口为由清理或重置。

## 下一步

从最新 `origin/main` 建立 A4-IOS-005 只读评估 worktree，比较“账户列表/详情读取副作用拆分”和“截图还款候选与确认”。先评估，不直接修改业务实现；沿 Spec → 红灯 → 最小实现 → macOS 验证顺序推进。
