# A2 CORE-009 实体归一化评估交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心实体归一化`

## 已完成

- 按治理指针链恢复 A2 当前状态，确认 CORE-008 已合并为 `58c11ad`。
- 只读审查 entity-normalizer、公开别名配置、7 项直接测试和 Edge 调用。
- 确认规则适合进入生产核心，但配置必须同步从 `tools` 迁入生产共享配置目录。
- 建立 CORE-009 边界评估、Spec、ADR-011、执行记录和本交接快照。
- 新增生产实体核心、共享公开配置、Lab wrapper、Node/Deno fixture、wrapper 和 Edge 静态契约。
- 本地 CORE-009、旧实体回归、边界检查和治理检查通过。

## 本地未验证与 CI 补齐

- Deno 双运行时未在本机验证；PR #59 Release Validation 后续已验证通过。
- 本地完整 Planner 受既有 `esbuild` 环境缺口影响；PR #59 CI 后续已验证完整 Planner 通过。
- PR #59 架构检查已确认生产 `tools/` 依赖计数，并于 2026-08-15 合并为 `f7b24cd`。

## 冻结范围

根工作区和其他 worktree 的 WIP、别名业务内容、数据库、生产部署、PWA/iOS 均不在本切片范围；本切片只改实体核心、共享配置、Lab wrapper、Edge import 和测试门禁。

## 下一步

CORE-009 已收口；由 CORE-010 通用域切片接续。
