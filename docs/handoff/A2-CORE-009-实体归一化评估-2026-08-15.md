# A2 CORE-009 实体归一化评估交接快照

> 日期：2026-08-15
>
> 分支：`docs/表达核心实体归一化边界评估`

## 已完成

- 按治理指针链恢复 A2 当前状态，确认 CORE-008 已合并为 `58c11ad`。
- 只读审查 entity-normalizer、公开别名配置、7 项直接测试和 Edge 调用。
- 确认规则适合进入生产核心，但配置必须同步从 `tools` 迁入生产共享配置目录。
- 建立 CORE-009 边界评估、Spec、ADR-011、执行记录和本交接快照。

## 未完成与未验证

- 尚未修改生产核心、共享配置、Lab wrapper 或 Edge import。
- 尚未建立 CORE-056 至 CORE-062 共享 Node/Deno fixture 和红灯。
- Windows/Deno 状态与完整 Planner 回归留给实现 PR 验证。

## 冻结范围

根工作区和其他 worktree 的 WIP、别名业务内容、数据库、生产部署、PWA/iOS 均不在本评估切片范围。

## 下一步

合并本评估文档后，从最新 `origin/main` 建立 `feature/表达核心实体归一化`，按红灯 → 最小迁移 → 双运行时/Planner 回归执行。
