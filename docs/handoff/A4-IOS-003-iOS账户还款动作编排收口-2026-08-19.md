# A4-IOS-003 iOS 账户还款动作编排收口

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-003 手动账户还款确认/撤销动作编排。
- 合并：PR #121，merge commit `df4265c`。
- 收口 Worktree：`D:\Business\count\.worktrees\A4-IOS-003收口`。
- 收口分支：`docs/A4-IOS-003收口`。

## 已完成

1. 新增纯 Swift `RepaymentActionUseCase`，不依赖 SwiftUI、HTTP、Supabase client 或数据库实现。
2. 同用户同账期同命令复用 Task；不同确认命令和确认/撤销交叉操作返回 `repayment_conflict`。
3. reset/user switch 使旧 Task stale，旧结果不刷新或写当前用户提示。
4. canonical cycle 在 accepted 后先收敛；账户/详情/dashboard 刷新失败单独返回，不把事务改写为失败。
5. AppState 保留确认、撤销、busy 和 message 的公开兼容入口，不再直接调用两个还款写 Repository 方法。
6. 截图还款、账户 CRUD、读取整体重构、数据库 RPC/migration、PWA、Edge 和 Planner 未进入 diff。

## 验证结果

- 红灯提交 `07ef9fe`：run `32264644828` 的 app build 通过，XCTest 因 Use Case 类型缺失按预期失败。
- 实现提交 `5e67af6`：run `32266597152` 的 macOS simulator build、完整 XCTest 和 iOS Build Gate 通过。
- 最终证据提交 `4db04e1`：run `32267345129` 再次通过 macOS Build/完整 XCTest和 iOS Build Gate。
- `npm run test:ios-repayment-action-boundary`：4/4 通过。
- A4-IOS-001/002 两组钱包快照源边界回归：各 4/4 通过。
- `npm run governance:check`、`npm run governance:arch`、`npm run build`：通过；仅有既有 bundle/eval 警告。
- Release Validation、Governance Validation、Cloudflare Pages 和 Vercel：通过。

## 未验证与剩余风险

- Windows 无 Swift/Xcode，本次 Swift 权威验证来自 GitHub macOS Actions。
- 未执行真实账户还款、生产迁移、生产部署或 TestFlight。
- `paid_at` 仍由 `AccountRepository` 生成，本片未改变既有时间语义。
- 截图还款、账户管理和读取副作用拆分仍需独立 Spec/TDD，不能直接复制本 Use Case。

## 下一步

1. 合并本收口文档 PR。
2. 从最新 main 只读评估 A4-IOS-004 最小切片，比较截图还款、账户读取副作用和账户管理原子边界。
3. A4-IOS-004 评估合并前不修改下一片业务实现。
