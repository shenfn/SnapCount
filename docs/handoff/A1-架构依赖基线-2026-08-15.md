# A1 架构依赖基线交接快照

> 任务：CLEAN-001/A1
>
> 日期：2026-08-15
>
> 分支：`docs/架构依赖基线`
>
> 基线：`3371a821e2df5d15967f2891a5a0d6e60ff35042`

## 已完成

- 重新读取 AGENTS、规格索引、阶段索引、治理规范和清洗总计划，确认当前任务是 A1，不进入 PWA/iOS 业务实现。
- 完成 Edge、PWA、iOS 和规则重复候选的只读盘点。
- 确认生产 Planner 对 `tools/ai-validation/expression-planner` 存在 12 条历史导入。
- 确认 PWA 页面层存在 5 项历史 Supabase/网络直连，`src/domains` 当前没有命中禁依赖扫描。
- 确认 PWA `useStore.js`、iOS `AppState.swift` 和 `NativeDataService.swift` 是后续模块化的主要边界对象。
- 增加 `scripts/check-architecture-boundaries.mjs` 和 `scripts/architecture-boundary-baseline.json`，采用“历史问题报告、新问题失败”的 ratchet。
- 增加 `docs/spec/架构依赖基线报告.md`，记录不变量、例外、后续归属和 A2 入口条件。

## 未验证

- 未运行 Deno 测试：当前 Windows 环境没有 `deno` 命令。
- 未运行 Swift 编译或 XCTest：当前环境为 Windows，必须由 GitHub macOS CI 验证。
- 未对线上 Edge 部署版本或生产迁移状态做查询；本阶段没有生产操作授权。
- RPC 契约统一注册表和规则重复分类尚未建立，当前仅作 warning/人工基线。

## 剩余风险

- 生产仍依赖 `tools/`；A2 迁移前不要移动或删除实验目录中的模块。
- PWA 页面和 `useStore` 仍有直连与集中编排；A3 必须保留兼容门面并逐切片回归。
- iOS 页面仍直接依赖 `AppState`；A4 不能以视觉重构代替业务编排收拢。
- 基线文件中的例外必须随每次迁移更新，不能把“ratchet 通过”误读为架构已完成。

## 下一步

1. 在本分支接入 `npm run governance:arch` 到本地脚本和 PR 治理 workflow。
2. 运行治理、架构检查和现有快速测试，区分业务、环境和未验证结果。
3. 完成 A1 后更新阶段索引，将当前任务切换为 A2 模块 Spec；不直接开始迁移代码。
4. 为 A2 编写 `expression-core` 模块 Spec 和最小特征测试。
