# A3 PWA 中转读取边界实现交接快照

> 任务：PWA-047 至 PWA-050
>
> 状态：规格已建立，红灯进行中
>
> 基线：`1d55f48`
>
> 分支：`feature/PWA中转读取边界`

## 已完成

- PR #80 已全门禁通过并合并，PWA-047 只读评估完成。
- 建立 `中转读取规格说明.md`，锁定 PWA-047 至 PWA-050 的实现范围。
- 决定只收拢开放/已处理中转查询与 DTO 映射，保留 Store 的加载编排。

## 冻结范围

- 根工作区及其他 worktree 的全部 WIP。
- 图片签名、还款候选、正式记录、账户边界、iOS、数据库、Edge 和生产配置。

## 当前验证

- staging discard 10 项、retry 8 项、archive 9 项、pending queue 已通过基线测试。
- 治理和架构检查已通过，仅保留既有人工清单警告。
- PWA-047 至 PWA-050 新增红灯尚未完成。

## 下一步

1. 为 Repository 开放/已处理列表、Store 接线和非目标边界写红灯。
2. 实现最小查询 transport 与 DTO 映射，保持 current user/run guard、图片签名和还款候选职责不变。
3. 运行专项、build、治理、安全和架构回归后提交 PR。
