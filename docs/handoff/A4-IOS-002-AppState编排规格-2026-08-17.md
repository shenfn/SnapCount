# A4-IOS-002 AppState 钱包快照动作编排规格交接

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-002 AppState 钱包快照动作编排规格。
- 基线：`128352a`，A4-IOS-002 评估 PR #116 合并提交。
- Worktree：`docs/A4-IOS-002-AppState编排规格`。
- 当前变更：只写正式 Spec、阶段索引、执行记录和交接；未修改 AppState、Repository、数据库、PWA、Edge、Planner 或生产配置。

## 已确认事实

1. A4-IOS-001 已通过 PR #115 的 macOS Build/XCTest，Repository 只调用 `apply_wallet_snapshot`。
2. AppState 仍直接编排钱包快照 session、busy、消息、刷新和用户切换时序。
3. A4-IOS-002 评估 PR #116 已合并，下一片只收拢 Wallet Snapshot Action Use Case。

## 下一步

1. 评审并合并本 Spec。
2. 从最新 main 建立 `test/中文短名` worktree，添加 A4-IOS-002A-F XCTest 红灯。
3. 用最小纯 Swift Use Case 实现 Task 复用、异目标冲突、generation stale 和 refresh failure 分层，再改 AppState 兼容门面。

## 冻结与风险

- Spec 合并前不修改 AppState；其他 AppState 事务不进入本片。
- accepted 事务不能因 refresh 失败而重发；旧请求不能更新当前用户状态。
- Windows 不能验证 Swift 编译，最终以 GitHub macOS iOS Build/XCTest 为准。
