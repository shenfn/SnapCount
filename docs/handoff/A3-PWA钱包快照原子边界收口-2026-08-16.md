# A3 PWA 钱包快照原子边界收口交接快照

> 任务：PWA-068A 至 PWA-068J 实现收口
>
> 状态：已完成
>
> 基线：`b20d89b`（PR #108 合并提交）
>
> 收口分支：`docs/PWA钱包快照原子边界收口`

## 已完成

- PR #108 已合并，merge commit 为 `b20d89b7b5a47d48a0493deeb3c81244234dfb35`。
- `apply_wallet_snapshot` 已成为 wallet 快照创建/关联、opening、账期、付款证据和 current-total 校准的数据库原子权威。
- Account Repository 只发送 record/account ID；Wallet Snapshot Feature 负责同命令复用、异目标冲突、会话 stale 和 accepted/refresh 分层。
- Store 已删除 wallet 快照的跨表多步写入；页面只在 accepted 后关闭选择器，并过滤归档或账户家族不兼容的目标。
- 历史 `repairEmptyAccountSnapshotBalances` 未重新接入读取链；PWA-069、iOS、Edge、Planner 和生产配置均未进入实现 diff。

## 验证证据

- 本地 wallet snapshot 专项 10 项、账户补绑 11 项、账户与还款相关回归 87 项通过。
- `npm run test:wallet-snapshot-contract`、PWA build、治理、架构、表达计划缓存契约和 `git diff --check` 通过。
- GitHub Actions run `31952369584` 在 PostgreSQL 17 连续执行 migration 两次，并验证 anon/跨用户、账户家族、幂等、opening、旧快照、账期优先级、付款 evidence、current-total adjustment 和中途异常回滚。
- 最终证据提交 `0f9dee3` 的 run `31952491558` 及全部适用门禁再次通过。

## 未验证与风险

- 未执行浏览器端手工点击验收；页面出口由源边界测试与 PWA build 覆盖。
- 未执行生产查询、生产 migration、部署、真实数据写入或 TestFlight。
- iOS 尚未接入同一 wallet snapshot RPC；该跨端收敛属于 A4，不在 PWA-068 回补。

## 冻结范围

- 根工作区和其他 worktree 的 WIP。
- iOS、Edge、Planner、生产配置、生产 migration、部署与通用存量 repair。
- PWA-069 在独立评估结论合并前只读，不直接修改流水 helper。

## 下一步

1. 从最新 `origin/main@b20d89b` 建立 PWA-069 流水 helper 只读边界评估。
2. 盘点 `upsertAccountEntry`、`voidAccountEntries` 及其消费者、吞错语义、RPC 权威和剩余直写，不修改业务代码。
3. 评估结论合并后再决定 PWA-069 是否需要实现片；A3 完成前 iOS 继续冻结。
