# A2 CORE-011 渲染契约实现交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心渲染契约`
>
> Worktree：`D:\Business\count\.worktrees\表达核心渲染契约`
>
> 基线：`b43d0a7`

## 已完成

- PR #62 全部门禁通过并合并，固定 CORE-011 Spec、ADR 和场景 CORE-077 至 CORE-084。
- 先新增共享 fixture、Node/Deno 测试和 wrapper/Edge 静态契约，两条 Node 命令均因生产模块缺失出现有效红灯。
- 整体迁移 render contract 到生产共享核心，Lab 改为薄 wrapper。
- Planner/Delivery/Shadow 改为引用生产权威，运行时版本常量收敛为一处。
- 架构基线最后一条生产 `tools` 例外已清空，SQL 历史 fixture 未修改。

## 已验证

- expression-core Node：10/10；wrapper：11/11；旧兼容：8/8。
- 旧 render：4/4；完整 Planner：185/185。
- PWA build、expression-core 边界、架构门禁、治理门禁和 `git diff --check` 通过。
- `production_tools_imports` 实测为 0。

## 未验证

- 本机无 Deno；Deno fixture、Edge `deno check`、Release Validation 由 PR CI 验证。
- 实现尚未提交、推送或合并；A2 完成证据和 fresh-agent 接续尚未执行。

## 剩余风险

- CI 若发现 Deno 与 Node 对 JSON/日期比较存在差异，必须保留失败证据并修正共享测试或实现，不能放宽场景断言。
- A2 在 CI、合并和阶段收口证据完成前仍保持“进行中”，不得开始 PWA/iOS 业务重构。

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- PWA/iOS 页面、数据库迁移、生产配置、部署和 TestFlight。
- Edge 查询、曝光持久化和交付编排；本切片仅改 import 与版本常量来源。

## 下一步

1. 逐文件检查并提交 CORE-011 实现。
2. 推送并创建 PR，等待全部 CI 通过后合并。
3. 从最新 `main` 单独建立 A2 阶段收口证据，运行严格 fresh-agent 接续。

## 相关入口

- Spec：`docs/spec/模块/表达核心/渲染契约规格说明.md`
- ADR：`docs/decisions/ADR-013-渲染与曝光契约归属.md`
- 执行记录：`docs/spec/执行记录/CORE-011-渲染契约边界评估.md`
- 共享 fixture：`supabase/functions/_shared/contracts/表达核心-渲染契约-v0.1.json`
- 生产核心：`supabase/functions/_shared/expression-core/render-contract.mjs`
