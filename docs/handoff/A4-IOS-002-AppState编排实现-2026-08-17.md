# A4-IOS-002 AppState 钱包快照动作编排实现交接

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-002 AppState 钱包快照动作编排实现。
- 远端基线：`6fbad5b`，PR #117 合并提交；本地因 Git HTTPS 连接重置，以等价 Spec 提交 `d5f6967` 建立 worktree。
- Worktree：`D:\Business\count\.worktrees\A4-IOS-002-AppState红灯`。
- 分支：`test/A4-IOS-002-AppState红灯`。

## 已完成

1. 新增 A4-IOS-002A-F XCTest，覆盖输入校验、Task 复用、异目标冲突、四种 canonical outcome、refresh failure、reset/user switch stale 和 AppState 兼容入口。
2. 新增纯 Swift `WalletSnapshotActionUseCase`；不依赖 SwiftUI、HTTP、Supabase client 或数据库实现。
3. AppState 的 create/link 公开方法保持不变，只负责 busy/message 投影和既有三个读模型刷新。
4. `resetUserScopedState()` 会使旧钱包快照 Task 失效；旧 Task 不再刷新或写入当前用户消息。
5. accepted 与 refresh failure 分层；刷新失败不会把已提交事务改成失败，也不会重发 Repository 写入。

## 本地验证

- `npm run test:ios-wallet-snapshot-action-boundary`：4/4 通过。
- `npm run test:ios-wallet-snapshot-boundary`：4/4 通过。
- `npm run governance:check`：通过。
- `npm run governance:arch`：通过，未新增架构违规。
- `npm run build`：通过；仅有既存 vconsole eval 和 chunk size 警告。
- `git diff --check`：通过；仅提示 Windows LF/CRLF 转换。

## 未验证与风险

- Windows 没有 Swift/Xcode，A4-IOS-002A-F 尚未真实编译执行；必须以 GitHub Actions macOS iOS Build/XCTest 为准。
- Swift 并发隔离、Task 复用测试的编译细节可能只在 macOS CI 暴露；CI 红灯属于实现问题，不能标记任务完成。
- 读模型刷新沿用现有 `loadAccounts`、`loadWalletSnapshots`、`refreshDashboard`，本片不重写读取架构。
- 未修改 Repository、数据库、PWA、Edge、Planner、页面视觉或生产配置；未执行部署、迁移或 TestFlight。

## 下一步

1. 逐文件暂存并提交本实现。
2. 使用无代理 Git 参数推送；若 Git HTTPS 仍重置，使用已验证的 GitHub API Git Objects 路径创建远端提交和 PR。
3. 等待 macOS iOS Build/XCTest、治理和综合门禁；按真实编译结果做最小修复。
4. 全部门禁通过后更新本记录为完成，再评估 A4 的下一个 AppState 编排切片。
