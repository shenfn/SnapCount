# A2 CORE-002 表达计划选择交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心降级选择`
>
> 规格：`docs/spec/模块/表达核心/降级选择规格说明.md`
>
> 执行记录：`docs/spec/执行记录/CORE-002-表达计划选择TDD执行记录.md`

## 已完成

- CORE-002 将表面容量、候选排序、覆盖排除、容量、多样性、周期冷却和两类 fallback 迁入 `supabase/functions/_shared/expression-core/plan-selection.mjs`。
- Edge Planner 已直接导入生产核心；Planner Lab 原路径保留为只 re-export 的兼容 wrapper。
- 共享 fixture 覆盖 CORE-008 至 CORE-011；独立 wrapper 测试覆盖 CORE-012。
- 架构 ratchet 的生产 `tools/` 依赖由 11 降到 10；未新增反向依赖。
- PR #46 CI 已通过 Node/Deno 选择测试、Edge 检查、完整 Planner 回归、治理、PWA 构建和迁移门禁。

## 当前边界

- `_shared/expression-core` 现在拥有第一切片的数值/事实契约和第二切片的表面选择/fallback。
- 候选生成、实体别名配置、资格门、评分、比较、跨记录关系和 render contract 仍保留在 Planner Lab 基线。
- 不删除 `tools/ai-validation`，不修改 PWA、iOS、Prompt、迁移或生产部署。

## 本地环境限制

- 当前 Windows 未安装 `deno`，因此 Deno 验证以 PR #46 GitHub Actions 为准。
- 当前 Windows 缺少 `esbuild`，完整 Planner 回归以 PR #46 的 CI 结果为准；本地已跑 expression-plan 的独立 8 项回归。

## 下一步

1. 对剩余 10 条生产 `tools/` 依赖做只读评估，选择下一组无 IO、低耦合、可建立旧行为 fixture 的规则。
2. 新切片必须建立独立中文 Spec、ADR（如有新归属决策）、fixture 和红灯，不能直接复制 CORE-002 的迁移步骤。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
