# A4-IOS-004 iOS 账户管理原子动作编排评估交接

更新时间：2026-08-19

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-004 下一最小业务切片只读评估。
- 基线：`f93b6fb`（PR #122 收口合并提交，当前 `origin/main`）。
- 评估 worktree：`D:\Business\count\.worktrees\A4-IOS-004业务边界评估`。
- 分支：`docs/A4-IOS-004业务边界评估`。
- 本次范围：只读核对与治理文档；未修改 iOS 业务实现、数据库、PWA、Edge 或 Planner。

## 决策

下一片选择“账户管理原子动作编排”，详见：

- `docs/spec/模块/iOS业务边界/账户管理原子动作编排边界评估.md`
- `docs/decisions/ADR-031-iOS账户管理接入数据库原子契约.md`

核心原因：iOS `AccountRepository.save/setArchived` 仍绕过已经存在的 `save_account` / `set_account_archived` canonical RPC；截图还款 transport 已走原子 RPC，账户读取已有分区失败基础。

编号约束：历史钱包快照规格已有无后缀场景 `A4-IOS-004`，不重写已合并证据；本任务后续场景统一使用 `A4-IOS-004A...`。

## 已核对证据

- `ios/SnapCount/Repositories/AccountRepository.swift`：保存是 REST insert/patch 加 `unsetOtherDefaults`；归档是 REST patch。
- `ios/SnapCount/App/AppState.swift`：保存/归档混合 session、busy、写入、列表/详情刷新和布尔结果，没有命令级 stale/refresh 分层。
- `ios/SnapCount/Repositories/InboxRepository.swift`：截图还款调用 `confirm_staging_repayment`。
- `ios/SnapCount/Repositories/AccountRepository.swift`：账户详情按流水、周期、支付分区读取并记录局部错误。
- `supabase/migrations/20260816160000_account_management_atomic_contract.sql`：canonical 账户保存/归档 RPC 已存在并返回账户行。
- PWA-066 账户管理 Spec、ADR-027：已确定跨端账户管理行为和测试映射。

## 下一步

1. 合并本评估 PR 后，从最新 `origin/main` 建立 A4-IOS-004 实现 worktree。
2. 先固定 Repository RPC transport 源边界，再写 `AccountManagementActionUseCase` 红灯。
3. 最小实现 Repository、Use Case、AppState 兼容接线；不改数据库和页面视觉。
4. 以 macOS GitHub Actions 的专项 XCTest 和完整 iOS Build 为 Swift 权威验证。
5. 收口时更新执行记录、阶段索引和新的交接快照。

## 未验证与剩余风险

- Windows 无 Swift/Xcode，本次未运行 iOS 编译或 XCTest。
- 未执行生产查询、迁移、部署、真实账户写入或 TestFlight。
- 实现前必须重新确认 `save_account` / `set_account_archived` RPC 签名、错误文本和 DTO 字段未被后续 main 提交改变。
- 根工作区和其他 worktree 的 WIP 未触碰，禁止以本交接为由清理或重置。
