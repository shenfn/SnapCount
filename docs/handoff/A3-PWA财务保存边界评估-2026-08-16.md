# A3 PWA 财务保存边界评估交接快照

> 任务：PWA-057
>
> 状态：待开始只读评估
>
> 基线：`24b4761`
>
> 计划分支：`docs/PWA财务保存边界评估`

## 已完成

- PWA-052 至 PWA-056 正式记录读取与已处理目标类型切片已合并。
- PWA-056 的 Node、PWA build、治理、架构和 PostgreSQL fixture 已通过两轮远程门禁。
- 正式记录边界评估已把财务保存 transport 列为下一独立切片。

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- 本轮只读，不修改 `useStore`、Repository、Feature、数据库迁移或测试实现。
- 不修改两个财务保存 RPC、账户流水/余额规则、删除、wallet、中转归档、图片签名、iOS、Edge、Planner、生产配置或部署。

## 当前验证基线

- 基线提交：`24b4761`。
- PR #85 两轮全门禁通过；本交接不新增业务行为。

## 待核对

- `confirmExpense` 与 `confirmIncome` 的参数组装、重复点击、current user/run guard 和本地状态收敛。
- `save_transaction_with_account` 与 `save_income_with_account` 的现有 transport 调用、错误映射和数据库权威边界。
- 写成功后账户/列表刷新失败、会话切换和 stale 结果的用户可观察出口。
- 可复用的 Record Repository/Feature 形态及最低合理测试层。

## 下一步

1. 从最新 `origin/main` 建立 `docs/PWA财务保存边界评估` worktree。
2. 只读运行基线测试并核对相关源码、Spec、ADR 和迁移契约。
3. 产出边界评估与候选场景编号；评估 PR 合并后再建立实现 Spec 和红灯。
