# A3 PWA 财务保存边界评估交接快照

> 任务：PWA-057
>
> 状态：只读评估完成，待文档 PR 门禁
>
> 基线：`81e0ed6`
>
> 计划分支：`docs/PWA财务保存边界评估`

## 已完成

- PWA-052 至 PWA-056 正式记录读取与已处理目标类型切片已合并。
- PWA-056 的 Node、PWA build、治理、架构和 PostgreSQL fixture 已通过两轮远程门禁。
- 正式记录边界评估已把财务保存 transport 列为下一独立切片。
- 已核对 modal manual/edit、staging、待补全、账户补绑四条不同保存路径及数据库 canonical RPC。
- 已产出 PWA-057 边界评估和 ADR-023，候选实现范围为 PWA-058 至 PWA-062。

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- 本轮只读，不修改 `useStore`、Repository、Feature、数据库迁移或测试实现。
- 不修改两个财务保存 RPC、账户流水/余额规则、删除、wallet、中转归档、图片签名、iOS、Edge、Planner、生产配置或部署。

## 当前验证基线

- 基线提交：`24b4761`。
- PR #85 两轮全门禁通过；本交接不新增业务行为。

## 评估结论

- 数据库 RPC 保持原子权威，首片不改迁移、账户流水或余额规则。
- Record Repository 包装两个 canonical RPC 并复用正式记录 DTO。
- Finance Save Feature 负责 inflight、generation、stale 和 `refreshStatus`。
- Store 保留校验、时间/账户选择和页面状态；manual/edit 不再直连 RPC 或手工拼 DTO。
- staging、待补全和账户补绑不进入首片；账户补绑另列 PWA-063。

## 下一步

1. 提交 PWA-057 评估文档并通过 PR 门禁。
2. 从合并后的最新 main 建立 PWA-058 至 PWA-062 实现 worktree。
3. 先建立实现 Spec/执行记录，再做 Repository/Feature/Store 有效红灯；不提前处理 PWA-063。
