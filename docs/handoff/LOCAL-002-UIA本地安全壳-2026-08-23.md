# LOCAL-002-UIA 本地安全壳交接

## 当前状态

- 阶段：L1 / LOCAL-002。
- worktree：`D:\Business\count\.worktrees\LOCAL-002-UIA本地安全壳红灯`。
- 分支：`test/LOCAL-002-UIA本地安全壳红灯`。
- PR：#158。
- 基线：`origin/main@277525c`。
- 状态：UIA 实现与首轮 macOS 绿灯完成；收尾文档与退出恢复测试待最后一轮 CI。

## 已完成

- 无 session 不再显示登录墙，进入 Records + 本地 Settings 两 Tab 安全壳。
- 已登录用户继续使用原五 Tab 云端壳。
- 未登录启动、刷新和切月通过 Local Use Case 读取本地消费。
- 本地 Records 不进入远端详情、不预取远端图片或详情，不展示账户/数据域和新增按钮。
- 本地 Settings 不加载云端设置，不提供登录按钮。
- 红灯 run `32625777973` 与绿灯 run `32626367969` 已形成可审计证据。

## 冻结范围

- 根工作区与其他 worktree WIP。
- UIB 账户准备和新增消费。
- 本地记录详情、编辑删除 UI、同步、AI Provider、其他数据域。
- PWA、Edge、Supabase、生产迁移、部署、TestFlight 和设计 Token。

## 下一步

1. 等待 PR #158 收尾提交的 macOS iOS Build、完整 XCTest 和治理门禁。
2. 全绿后将 PR 转为 Ready 并合并。
3. 从最新 main 新建 UIB 红灯 worktree，先固定显式账户准备、Decimal 期初余额、账户必选与表单网络调用为 0。

## 未验证与风险

- 未做真机交互和视觉截图验收；本轮由源边界与 SwiftUI 构建验证结构和行为。
- UIA 不提供创建入口是刻意边界，不是功能遗漏；UIB 完成后再开放。
- L1 不包含登录同步，本地 Settings 的“云同步尚未开启”不可改成普通登录入口。
