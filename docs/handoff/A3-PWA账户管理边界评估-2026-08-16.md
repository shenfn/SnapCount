# A3 PWA 账户管理边界评估交接快照

> 任务：PWA-066
>
> 状态：只读评估完成，待文档 PR 验证
>
> 基线：`06dde1b`
>
> 分支：`docs/PWA账户管理边界评估`

## 上游已完成

- PWA-065A 至 PWA-065E 已由 PR #98 合并，账户列表与详情纯读取、stale、分区失败和刷新结果已有测试。
- 账户读取实现收口 PR #99 已合并，merge commit `06dde1b`，PWA-066 已登记为下一只读任务。
- ADR-025 已要求账户默认项先裁决原子性；ADR-026 已禁止把写副作用放回读取 Repository。

## 本轮已完成

- 只读确认 PWA 默认项为多步写入，失败仍显示成功；数据库无默认互斥与归档默认约束。
- 只读确认归档账户没有稳定恢复入口，保存后列表与当前详情可能分叉。
- 只读确认账户管理写命令没有 user/generation stale 或 accepted/refresh 分层。
- 只读确认 iOS 有归档列表但共享同一多步默认写入风险，不能只修 PWA。
- 已新增账户管理边界评估、ADR-027、执行记录，并拆出 PWA-066A 至 PWA-066F。

## 冻结范围

- 根工作区与其他 worktree 的全部 WIP。
- 本评估分支只允许文档；不修改 PWA/iOS/数据库/Edge/Planner 或生产状态。
- wallet 快照创建/关联/repair、截图还款、流水 helper、单账户物理删除和 iOS AppState 重构继续冻结。

## 下一步

1. 先完成本评估文档的本地治理检查、提交和 PR 门禁。
2. PR 合并后建立独立收口文档分支，登记远程证据并把当前任务切到 PWA-066A 至 PWA-066F 实现。
3. 从收口后的 main 建实现 worktree，先提交正式 Spec，再写 PostgreSQL migration fixture 红灯。
4. 先证明旧客户端直写兼容与默认/归档原子契约，再接 Repository、Feature、Store/Page；不进入 wallet repair。

## 未验证与风险

- 生产数据中是否已有多默认或归档默认未知；本轮未连接生产服务。
- canonical RPC、trigger、约束和迁移 fixture 尚未实现。
- PWA 归档列表/恢复交互和 iOS A4 接线尚未开始。
- 类型跨家族阻断与自动扣款引用清理尚无数据库测试证据。

## 验证摘要

- `test:account-read` 17 项、`test:repayment` 15 项、`test:account-binding` 11 项通过。
- Vite build 和架构检查通过，仅既有警告。
- 未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
