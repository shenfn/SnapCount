# A2 CORE-008 跨记录关系评估交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心跨记录关系`

## 已完成

- 按 `AGENTS.md` → 规格索引 → 阶段索引 → 清洗计划恢复当前状态。
- 只读审查 cross-record 实现、4 项直接测试、Planner 两个入口及 Shadow/Delivery 来源装配。
- 确认其为独立纯函数，建立边界评估、CORE-008 Spec、ADR-010 和执行记录。
- 登记 `timeZone` 兼容参数、同域候选现状和来源字段投影三个不改变的边界。
- 新增生产核心实现、Lab wrapper、Node/Deno fixture、wrapper 兼容测试和 Edge 生产 import。
- 本地 CORE-008、旧 cross-record、前四片核心回归、边界检查和治理检查通过。

## 未完成与未验证

- Deno 双运行时结果尚未在本机验证，须由 PR CI 验证。
- 完整 Planner 仍有 5 个旧测试因缺少 `esbuild` 无法启动。
- 尚未确认 CI 中生产 `tools/` 依赖计数从 5 降至 4。

## 冻结范围

根工作区和其他 worktree 的 WIP、数据库、生产部署、PWA/iOS 业务架构均不在本切片范围；本切片只改生产共享纯函数、Lab wrapper、Edge import 和测试门禁。

## 下一步

等待实现 PR 的 Node/Deno、Edge、Planner 和治理门禁；通过后合并并收口 CORE-008，再评估下一片纯函数。
