# A2 CORE-008 跨记录关系评估交接快照

> 日期：2026-08-15
>
> 分支：`docs/表达核心跨记录边界评估`

## 已完成

- 按 `AGENTS.md` → 规格索引 → 阶段索引 → 清洗计划恢复当前状态。
- 只读审查 cross-record 实现、4 项直接测试、Planner 两个入口及 Shadow/Delivery 来源装配。
- 确认其为独立纯函数，建立边界评估、CORE-008 Spec、ADR-010 和执行记录。
- 登记 `timeZone` 兼容参数、同域候选现状和来源字段投影三个不改变的边界。

## 未完成与未验证

- 尚未修改生产核心、Lab wrapper 或 Edge 导入。
- 尚未创建共享 Node/Deno fixture 和红灯提交。
- Windows 未安装 Deno，双运行时结果须由 PR CI 验证。

## 冻结范围

根工作区和其他 worktree 的 WIP、数据库、生产部署、PWA/iOS 业务架构均不在本切片范围。

## 下一步

合并本评估文档后，从最新 `origin/main` 创建 `feature/表达核心跨记录关系`，按 CORE-008 的红灯 → 最小迁移 → Node/Deno/Planner 回归顺序执行。
