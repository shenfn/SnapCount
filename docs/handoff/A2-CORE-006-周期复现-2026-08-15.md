# A2 CORE-006 周期复现交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心周期复现`
>
> 规格：`docs/spec/模块/表达核心/周期复现规格说明.md`
>
> 执行记录：`docs/spec/执行记录/CORE-006-周期复现TDD执行记录.md`

## 已完成

- 已完成 recurrence 只读边界评估，确认它是独立纯函数切片，优先于 419 行的 comparison。
- 已建立 CORE-006 Spec、ADR-008、共享 Node/Deno fixture 和断言器。
- 红灯已验证：生产核心目标文件不存在时 Node 测试以 `ERR_MODULE_NOT_FOUND` 失败。
- 最小实现已完成：生产核心、Lab wrapper、Edge 导入、Node/Deno workflow 和 ratchet 均已更新。
- 本地 Node fixture、wrapper、旧 8 项回归、前五片核心回归、边界和治理检查均通过；生产 `tools/` 依赖降为 6 项。
- GitHub PR #51 的 Node/Deno、Edge、完整 Planner、治理、PWA 和迁移门禁均已通过，并已合并为 `a272a6c`。

## 当前边界

- 目标只有 `generateRecordNameRecurrenceCandidates`。
- 不修改 comparison、cross-record、generic-domain、实体配置、PWA、iOS、数据库或生产部署。
- 根工作区 WIP 和其他 worktree 继续冻结，不做清理或归档。

## 下一步

1. 只读评估 comparison 的日基线、周同比和类别对比边界。
2. 若需要拆分，先分别建立独立 Spec 和场景编号，不把 419 行实现整块迁移。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
