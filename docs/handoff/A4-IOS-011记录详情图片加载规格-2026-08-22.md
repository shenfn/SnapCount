# A4-IOS-011 记录详情图片加载规格交接快照

> 日期：2026-08-22
>
> 分支：`docs/A4-IOS-011记录详情图片加载规格`
>
> 基线：`origin/main@3e680ba`

## 已完成

- A4-IOS-011 边界评估 PR #144 已合并。
- 建立本模块 Spec，锁定 `RecordDetailImageUseCase` 的输入、结果、并发、session/generation stale 和 AppState 投影边界。
- 明确完整详情读取、表达计划、图片签名算法和财务事实不进入本片。

## 当前状态

- 仅有 Spec、索引和交接文档，尚未修改 Swift 代码。
- 下一分支应从最新 `origin/main` 建立 `test/A4-IOS-011记录详情图片红灯`。

## 验证

- 本阶段将运行 `git diff --check`、`npm run governance:check` 和 `npm run governance:arch`。
- Windows 不运行 Swift/XCTest；红灯和最终绿灯以 GitHub macOS iOS Build/XCTest 为准。

## 未验证与剩余风险

- 当前基线尚无图片 hydration 专项 Use Case 测试。
- 现有 AppState 的图片 hydration 仍是私有直编排，红灯阶段需要证明并发、reset 和 stale 缺口。
- 未授权修改图片签名服务缓存、bucket、重试、数据库、Edge、Planner、PWA 或视觉。

## 下一步

1. 提交并合并本 Spec PR。
2. 从最新主线建立红灯分支，先固定 A4-IOS-011A-G。
3. 红灯证据通过后才进入最小 Use Case 和 AppState 投影实现。
