# A3 PWA 中转原子归档接线准备交接快照

> 任务：PWA-040
>
> 状态：待开始
>
> 日期：2026-08-16
>
> 目标分支：`feature/PWA中转原子归档`
>
> 基线：`e492283`

## 已完成前置

- PWA-033 只读评估已由 PR #74 合并。
- PWA-034 至 PWA-039 PostgreSQL 契约和域校验迁移已由 PR #75 合并。
- expense、income、通用域、账户流水、同域幂等、金额拒绝、跨用户、anon、故意失败回滚和不同域重试均有数据库证据。

## 本切片目标

- 在现有 `src/repositories/stagingRepository.js` 增加原子归档 RPC 传输入口。
- 在现有 `src/features/staging/` 增加归档编排、并发共享和刷新失败出口。
- 将 `useStore.archiveStagingRecord` 收窄为兼容门面。
- 删除客户端直接创建正式目标、更新中转终态、写路由反馈和财务 `0.01` 兜底。

## 冻结区

- 根工作区及其他 worktree 的全部 WIP。
- iOS、数据库迁移、Edge、生产配置、生产数据和迁移历史修复。

## 验证要求

- 先写 Repository/Feature 红灯测试，再实现最小接线。
- 覆盖同 ID 重复点击、金额缺失、RPC 成功后刷新失败、用户切换和旧请求晚到。
- 运行中转归档/重试、pending queue、PWA build、治理、安全和架构门禁。
