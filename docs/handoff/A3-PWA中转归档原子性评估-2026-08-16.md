# A3 PWA 中转归档原子性评估交接快照

> 任务：PWA-033
>
> 状态：历史评估交接；PR #74 已合并，由数据库契约交接接续
>
> 日期：2026-08-16
>
> 分支：`docs/PWA中转归档原子性评估`
>
> 基线：`4ee12f7`

## 已完成

- 确认数据库已有原子归档 RPC，iOS 与 PWA 部分路径已采用。
- 确认 PWA 快速/财务路径仍绕过 RPC，并存在跨表半状态。
- 确认 PWA 缺失金额 `0.01` 兜底会制造无证据财务事实。
- 确认现有 PostgreSQL fixture 对原子归档行为覆盖不足。
- 只读核对生产迁移列表和函数关键语义，没有执行写操作。
- 建立 PWA-033 至 PWA-040 规格和 ADR-018。

## 冻结区

- 根工作区及其他 worktree 的全部 WIP。
- 本评估不修改数据库迁移、PWA/iOS 业务代码、Edge、部署配置或生产数据。
- 不执行 migration repair、历史孤儿修复或真实归档。

## 当前结论

- 先证明数据库契约，再迁移 PWA，不能反过来。
- 复用 `archive_staging_record`，不创建客户端补偿事务。
- 迁移版本号不一致继续登记，但线上函数能力已由只读查询确认。

## 下一步

1. 当前状态改读 `docs/handoff/A3-PWA中转归档数据库契约-2026-08-16.md`。
2. 只有数据库行为门禁通过后，才进入 PWA Repository/Feature 接线。
