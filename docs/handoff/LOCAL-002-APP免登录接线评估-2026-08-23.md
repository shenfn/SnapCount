# LOCAL-002-APP 免登录接线评估交接

## 当前状态

- 阶段：L1 / LOCAL-002。
- worktree：`D:\Business\count\.worktrees\LOCAL-002-APP免登录接线评估`。
- 分支：`docs/LOCAL-002-APP免登录接线评估`。
- 基线：`origin/main@e68a64d`。
- 当前批次：LOCAL-002-APP 评估与正式 Spec 完成，尚未改 Swift 业务代码。

## 已完成

- 复核 AppState 启动、消费新增/编辑/删除、历史月份读取和当前远端 Repository 接线。
- 复核 LocalDatabase、LocalExpenseRepository、LocalModels 和 PORT 导入导出边界。
- 固定 profile lifecycle、network gate、mapper、local read model 和 AppState 兼容门面的边界。
- 新增 `docs/spec/模块/iOS本地优先/消费记录App免登录接线评估.md`。
- 新增 `docs/spec/执行记录/LOCAL-002-APP免登录接线评估.md`。
- 新增 `docs/spec/模块/iOS本地优先/消费记录App接线规格说明.md`，固定接口形状、场景和 DoD。

## 冻结范围

- 根工作区和其他 worktree WIP。
- PWA、Edge、Supabase、生产数据库、发布配置和 TestFlight。
- AppState、SwiftUI 页面和远端 Repository 的实际代码接线，直到 APP 红灯规格完成。

## 下一步

1. 在独立 `test/LOCAL-002-APP...` 分支添加 profile lifecycle、network spy、Draft mapper 和本地读模型的红灯。
2. 取得 macOS CI 有效红灯后，按 profile store -> mapper/read model -> Use Case -> AppState 接线顺序实现。
3. 继续保持本分支与 AppState、页面、Supabase 和发布配置隔离。

## 未验证与风险

- Windows 不能验证 Swift 编译；必须依赖 GitHub macOS CI。
- 默认本地账户的产品交互仍需单独决定；本片不隐式创建账户。
- 登录绑定、同步、云端老用户迁移、AI/BYOK 仍不在范围内。
