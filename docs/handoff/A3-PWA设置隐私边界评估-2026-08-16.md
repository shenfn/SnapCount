# A3 PWA 设置与隐私边界评估交接快照

> 任务：PWA-008
>
> 状态：当前权威交接快照
>
> 日期：2026-08-16
>
> 分支：`docs/PWA设置隐私边界评估`
>
> 基线：`52aa92a`（PR #66 合并提交）

## 已完成

- PR #66 已合并，PWA-001 表达计划与反馈边界正式完成。
- 只读盘点 Store、PageSettings、PageAiVisionSettings、ModalWelcome、AuthPage、迁移与安全测试。
- 确认“设置与隐私”必须拆为配置、隐私副作用、数据导出和认证，不做大 Feature。
- 建立设置与隐私边界评估、PWA-008 首片 Spec、ADR-015 和执行记录。

## 当前决策

- 首实现片只统一 `user_configs` Repository/Feature、共享状态、legacy fallback 和失败回滚。
- retention 保存失败后不得继续立即清理。
- PageSettings 的导出、cleanup fetch 和 signOut 暂留；AuthPage 归后续认证边界。
- 页面直连基线预计 5→3，只按实现真实移除项收紧。

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- PWA 业务代码、页面视觉、数据库、Edge、iOS、生产配置、部署和 TestFlight。
- 本文档切片不创建 Settings Feature/Repository，不修改架构 baseline。

## 验证基线

```text
registration consent helper：1/1 通过
security contracts：通过
governance:arch：通过（0 / 5 / 0）
```

## 未验证与风险

- Settings Feature/Repository 尚未实现，用户切换、竞态和 rollback 需 TDD 证明。
- 导出错误处理与原图清理不可逆副作用只登记，未修复。
- GitHub PR 门禁尚未运行。

## 下一步

1. 更新索引和 PWA-001 收口状态，运行治理与差异检查。
2. 提交并创建纯文档 PR。
3. PR 合并后建立 `feature/PWA设置配置边界`，先写红灯，不直接修改其他业务边界。
