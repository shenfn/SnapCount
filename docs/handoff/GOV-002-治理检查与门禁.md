# GOV-002 治理检查与门禁交接快照

> 任务编号：GOV-002
>
> 日期：2026-08-15
>
> 分支：`docs/项目治理与进度保护`
>
> Worktree：`D:\Business\count\.worktrees\项目治理与进度保护`

## 已完成

- 从 `origin/main@49614c8` 创建干净治理 worktree。
- 在 `AGENTS.md` 中补充规格、阶段、ADR 和交接入口。
- 新增 `03-阶段与任务索引.md` 和 `04-架构治理与进度保护规范.md`。
- 新增 ADR 模板、交接快照模板和冻结根工作区 ADR。
- 新增 `scripts/check-project-governance.mjs` 与 `npm run governance:check`。
- 新增 PR/标准分支 push 触发的治理检查 workflow。

## 当前状态

- 当前阶段：Phase 2，本地治理检查与 PR 门禁。
- 已验证基线：`origin/main@49614c8`。
- 本地验证：`npm run governance:check` 通过；`node --check scripts/check-project-governance.mjs` 通过；`git diff --check` 通过。
- Fresh-agent 演练：首次发现阶段状态冲突，修正后第二次通过；入口链判定为完整。

## 未验证

- GitHub Actions 上的治理 workflow 尚未运行。
- Edge Function 的线上部署版本尚未确认；现有快照脚本会使用 service-role 拉取多表数据，未执行。
- 尚未提交、推送或创建 PR。

## 剩余风险

- 现有历史 `codex/*` 分支未迁移，只由检查脚本发出警告。
- 根工作区和其他 worktree 的 WIP 仍未完成负责人和 PR 归属确认。
- 当前治理检查验证入口完整性，不验证业务架构依赖方向。

## 冻结范围

- 根工作区和其他 worktree 不在本任务中修改。
- 不修改生产业务代码、数据库迁移、Edge Function、Planner 算法或 iOS 页面。
- 不执行生产部署、迁移、TestFlight、远端分支删除或强推。

## 下一步

1. 重新运行 `npm run governance:check` 和 fresh-agent 接续演练。
2. 逐文件检查 staged 内容，确认仅包含治理文件。
3. 经用户确认后提交并创建 PR，等待 GitHub workflow 验证。
4. PR 通过后再开始 `lint:arch` 设计和依赖方向基线。

## 相关入口

- 总规范：`docs/spec/规格文档索引.md`
- 当前阶段：`docs/spec/03-阶段与任务索引.md`
- 治理规范：`docs/spec/04-架构治理与进度保护规范.md`
- ADR：`docs/decisions/ADR-001-冻结根工作区并使用独立治理worktree.md`
- 测试或 CI 证据：`npm run governance:check`、`.github/workflows/governance-validation.yml`
