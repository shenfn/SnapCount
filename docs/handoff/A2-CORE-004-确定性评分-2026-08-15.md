# A2 CORE-004 确定性评分交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心确定性评分`
>
> 合并提交：`b4da010`（PR #48）
>
> 规格：`docs/spec/模块/表达核心/确定性评分规格说明.md`
>
> 执行记录：`docs/spec/执行记录/CORE-004-确定性评分TDD执行记录.md`

## 已完成

- CORE-004 已将确定性评分、曝光隔离、偏好边界、表面阈值和排序摘要迁入 `supabase/functions/_shared/expression-core/deterministic-scoring.mjs`。
- Edge Planner 已直接导入生产核心；Planner Lab 原路径保留为只 re-export 的兼容 wrapper。
- 共享 fixture 覆盖 CORE-018 至 CORE-022；独立 wrapper 测试覆盖 CORE-023。
- PR workflow 已加入评分 Node/Deno 双运行时检查。
- 架构 ratchet 的生产 `tools/` 依赖由 9 降至 8，旧评分导入已从例外清单移除。

## 当前边界

- `_shared/expression-core` 现在拥有数值/事实契约、表面选择/fallback、候选资格门和确定性评分。
- 候选生成、实体别名配置、比较、跨记录关系和 render contract 仍留在 Planner Lab 基线。
- 不删除 `tools/ai-validation`，不修改 PWA、iOS、Prompt、迁移或生产部署。

## 验证状态

- CORE-004 Node 共享 fixture、Lab wrapper、旧评分 9 项回归、资格和选择回归均已通过。
- 纯函数边界、治理入口和架构 ratchet 已通过。
- PR #48 的 Node/Deno、Edge、完整 Planner、治理、PWA 和迁移门禁均已通过。
- Windows 未安装 Deno，且缺少完整 Planner 回归所需的 `esbuild`；本地结果以 PR #48 CI 补齐。

## 下一步

1. CORE-004 已完成；从最新 `origin/main` 新建 CORE-005 worktree。
2. 只读评估 `fact-candidates.mjs`，先建立独立 Spec、fixture 和红灯，再迁移。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
