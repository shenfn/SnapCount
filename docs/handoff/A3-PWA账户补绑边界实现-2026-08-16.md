# A3 PWA 账户补绑边界实现交接快照

> 任务：PWA-063A 至 PWA-063F
>
> 状态：本地实现与回归完成，待提交和远程门禁
>
> 基线：`30aed05`
>
> 分支：`feature/PWA账户补绑边界实现`

## 上游已完成

- PWA-063 评估 PR #90 已通过全部门禁并合并，merge commit `30aed05`。
- ADR-024 已决定复用 Record Repository，并建立独立 Account Binding Feature。
- 财务保存、正式记录读取、发生时间、账户选项、构建与治理基线均已通过。

## 当前范围

- PWA-063A 至 PWA-063F 的红灯、Feature 最小实现、Store/Page 接线与回归。

## 已完成

- 有效红灯已证明 Feature 缺失、Store direct RPC 和批量 Store 循环仍存在。
- Account Binding Feature 已实现 DTO 映射、同请求合并、冲突拒绝、generation、stale 和刷新分层。
- 单笔与批量 Store 接线已使用 canonical record 收敛，成功项会从未绑定集合移除。
- 批量已返回逐项结果与成功/失败计数，只执行一次写后刷新。
- `resetUserData`、发生时间契约、专项 npm 脚本和 Release Validation 已接线。
- 推荐、预览、默认账户、RPC、迁移、账户流水和余额规则均未修改。

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- 数据库 RPC、迁移、账户流水、余额方向、推荐算法、默认账户、manual/edit、staging、待补全、wallet、删除、图片签名、iOS、Edge、Planner、生产配置和部署。

## 下一步

1. 最终审查 diff 与暂存区，逐文件提交实现。
2. PR #91 首轮 Shadow 184/185 的旧缓存静态契约已对齐 accepted 收敛边界；推送修复后等待第二轮完整门禁。
3. 全部门禁通过后合并；另开文档收口切片，不在实现分支追加下一业务边界。

## 验证摘要

- `test:account-binding` 11 项、`test:finance-save` 16 项、`test:record-read` 14 项通过。
- staging 四组、发生时间、账户选项、wallet、表达展示和安全契约通过。
- 构建、治理和架构检查通过；构建只有既有 `eval` 与 bundle size 警告。
- PR #91 首轮新增账户补绑专项通过；Shadow 184/185 的唯一旧静态契约已修正，定向 3 项通过。
- Windows 全量 Shadow 剩余 5 项因缺少 `esbuild` 归类为环境失败，等待 GitHub 完整复核。
- 未连接真实服务执行账户补绑。
- 未执行生产查询、迁移、部署、真实数据写入或 TestFlight。
