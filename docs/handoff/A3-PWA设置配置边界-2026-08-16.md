# A3 PWA 设置配置边界交接快照

> 任务：PWA-008
>
> 状态：PR #68 已合并，PWA-008 完成
>
> 日期：2026-08-16
>
> 分支：`feature/PWA设置配置边界`
>
> 基线：`00d5c1c`（PR #67 合并提交）

## 已完成

- Settings Repository/Feature 和配置映射已实现。
- `useStore` 保留兼容公开 API，新增批量 `setSettings`，保留用户切换 reset、请求去重和旧响应保护。
- PageSettings、PageAiVisionSettings、ModalWelcome 不再直接访问 `user_configs`。
- retention 只在配置保存返回 `{ ok: true }` 后允许进入 `cleanup_all_images`。
- 架构 ratchet 已由 5 项收紧到 3 项，AuthPage 和 PageSettings 的导出/清理/退出边界仍显式保留。

## 当前范围与冻结区

- 只修改当前 `feature/PWA设置配置边界` worktree。
- 根工作区和其他 worktree 的所有 WIP 继续冻结，不清理、不重置、不归类。
- 不修改数据库迁移、Edge Function、Planner、iOS、生产配置、部署或 TestFlight。

## 验证基线

- 设置与页面契约：14/14。
- 表达回归：22/22，表达展示：22/22。
- 财务时间、安全契约、治理检查、架构检查：通过。
- PWA build：通过，157 modules transformed。
- 注册同意服务端契约：4 项通过，临时账号已清理。

## 未验证项与风险

- PR #68 综合、治理、架构和预览门禁已通过并合并，merge commit 为 `21e011b`。
- 导出查询错误和原图清理 transport 不在本片。
- legacy schema 下新增 vision 列更新会失败并由 Store 返回错误，这是兼容限制，不应在页面复制 fallback。

## 下一步

1. 保留当前 worktree 和分支，不删除历史对象。
2. 当前任务转入 PWA-026 中转生命周期边界评估。
