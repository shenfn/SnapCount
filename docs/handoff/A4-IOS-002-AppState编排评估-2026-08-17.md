# A4-IOS-002 AppState 钱包快照动作编排评估交接

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-002 AppState 钱包快照动作编排边界评估。
- 基线：`61874ba`，PR #115 已合并并通过 GitHub macOS Build/XCTest。
- Worktree：`docs/A4-IOS-002-AppState编排评估`。
- 当前变更：只读评估、阶段索引、规格索引和本交接；未修改 AppState、Repository、数据库、PWA、Edge、Planner 或生产配置。

## 已完成

1. A4-IOS-001 已把 iOS 钱包快照 create/link 统一到 `apply_wallet_snapshot`，显式保留 outcome 和 `needs_confirmation`。
2. A4-IOS-001 PR #115 的 macOS simulator build、unit tests、PWA/Edge 和治理门禁全部通过。
3. 评估确认 AppState 当前仍混合 session、busy、generation 缺口、结果文案和刷新编排。

## 下一步

1. 将本评估转为 A4-IOS-002 正式 Spec，固定 Use Case 输入/输出、generation stale、refresh failure 和兼容门面边界。
2. 建立 XCTest 红灯，再提取最小 `WalletSnapshotActionUseCase`，保持 AppState 公开入口。
3. 通过 macOS Build/XCTest 后，才评估是否将同一编排模式扩展到其他 AppState 事务。

## 冻结与风险

- A4-IOS-002 Spec 合并前不修改 AppState；根工作区和其他 worktree WIP 继续冻结。
- 不能把 refresh 失败重试写操作；事务 accepted 与读模型 refresh 必须分层。
- 用户切换时旧请求不能更新当前用户的 message/数组；generation guard 是本片核心验收。
