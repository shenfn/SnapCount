# C2 根 WIP 分簇保全交接记录

> 任务：CLEAN-001/C2
>
> 日期：2026-08-15
>
> 基线：`origin/main@6d36bf1`
>
> 工作分支：`docs/根WIP分簇保全`

## 目标与边界

本阶段只保全根工作区 `D:\Business\count` 的未提交内容，不在根工作区或其他 worktree 修改、重置、清理、删除、提交或部署。每个文件先与 `origin/main` 比较，再决定是否导出补丁或建立独立保全分支。

## 当前事实

- 根工作区分支为 `main@2a438d14`，相对 `origin/main` 落后约 49 个提交。
- 根工作区有 8 个已修改 tracked 文件和 17 个未跟踪文件，共 25 个 WIP 对象。
- C1 已完成登记，但“已合并”不等于允许删除；本记录不授予任何删除授权。
- PR #25 是唯一继续评估的 iOS 首页候选；`ios-home-v3-native` 不再推进。
- `system-spec-overview` 的实际分支为 `codex/record-lifecycle-tdd-plan`，内容属于 REC-009 中转站工作线；REC-009 暂缓，不接入 C2。

## 分簇登记

| 分簇 | 根工作区内容 | 与 `origin/main` 的关系 | 当前处置 | 风险/下一步 |
|---|---|---|---|---|
| Personal Context / prompt 契约 | `supabase/functions/ingest-receipt/index.ts`、`prompts.ts`、`signals.ts`、`signals_test.ts`；未跟踪 `context-packet.ts`、相关测试、两份 personal-context 文档 | tracked 文件为本地修改；未跟踪代码和文档与上游同名文件内容不同；`20260806100000_personal_context_memory_contract.sql` 与上游完全相同，不重复保全 | 待导出为独立 feature 分支/补丁，先做代码和迁移依赖审查 | 共享 `release-validation.yml` 只保留对应 hunk；这是生产 prompt 契约变更，需单独测试和回滚点 |
| Security contract | `scripts/check-security-contracts.mjs`、`scripts/security-migration-fixture.sql`、`scripts/test-security-migration.sql`，以及 `release-validation.yml` 的安全测试 hunk | 4 个文件为本地修改；workflow 与 Personal Context 共享，不能整文件复制 | 待导出为独立 fix 分支/补丁 | 必须确认 fixture 与迁移契约一致；不执行生产迁移 |
| 演示账号造数 | 根目录 `scripts/seed-demo-data.mjs` | 未跟踪，且不是 `demo-data-seed` worktree 中已审查的 30 天安全版本 | 不运行、不作为权威脚本；后续只审查 `demo-data-seed` 版本 | 需要预检、失败回滚、余额一致性和算法表零新增证据；造数前须单独授权 |
| 素材与运维 | iOS 提案 HTML、推广文案/分镜/视频、参考图、`scripts/pull-live-expression-snapshot.mjs` | 均为根工作区未跟踪文件；不属于生产代码 | 素材只登记，不混入业务分支；线上快照脚本冻结待安全审查 | 二进制文件不在本阶段复制；快照脚本含 service-role 能力，未经审查不得执行 |

## 明确排除

- `supabase/migrations/20260806100000_personal_context_memory_contract.sql` 已与 `origin/main` 内容相同，不创建重复迁移。
- 不把根目录旧版造数脚本与 `demo-data-seed` 的安全版本合并。
- 不把 iOS 首页 v3、REC-009 或推广素材带入架构试点。
- 不修改 `origin/main`、生产 Supabase、Edge Function 部署、TestFlight 或远端分支。

## 逐文件清单

### Personal Context / prompt 契约

```text
supabase/functions/ingest-receipt/index.ts          (tracked modified)
supabase/functions/ingest-receipt/prompts.ts        (tracked modified)
supabase/functions/ingest-receipt/signals.ts        (tracked modified)
supabase/functions/ingest-receipt/signals_test.ts   (tracked modified)
supabase/functions/ingest-receipt/context-packet.ts
supabase/functions/ingest-receipt/context-packet_test.ts
supabase/functions/ingest-receipt/prompts_test.ts
docs/personal-context-architecture-v0.1.md
docs/personal-context-development-log-v0.1.md
supabase/migrations/20260806100000_personal_context_memory_contract.sql (与上游相同，排除)
```

### Security contract

```text
.github/workflows/release-validation.yml
scripts/check-security-contracts.mjs
scripts/security-migration-fixture.sql
scripts/test-security-migration.sql
```

### 演示数据、素材和运维

```text
scripts/seed-demo-data.mjs
scripts/pull-live-expression-snapshot.mjs
docs/ios-home-redesign-proposal-v0.1.html
kimetsu_muichiro_vs_kokushibo.mp4
kokushibo_reference.jpg
kokushibo_reference_v2.jpg
promo_shot01_seed.mp4
promo_shot02_scatter.mp4
芥子推广文案_10选题全案_v1.0.md
芥子预热推广_分镜_v1.0.md
芥子预热推广_竖屏15s_v1.mp4
```

以上清单含 8 个 tracked 修改、17 个未跟踪对象；迁移文件虽在根目录未跟踪，但因内容已与上游相同，不进入保全补丁。

### 共享 workflow 的 hunk 归属

`.github/workflows/release-validation.yml` 不能按整文件复制，当前根 WIP 只登记以下两类 hunk：

- Personal Context：receipt safeguards 测试命令新增 `context-packet_test.ts`、`prompts_test.ts`，安全检查 job 新增 `20260806100000_personal_context_memory_contract.sql`。
- Security contract：同一安全检查 job 保留 `scripts/test-security-migration.sql` 的既有调用位置；对应 fixture 和断言由三个 `scripts/` 文件承载。

后续拆分时必须逐 hunk 重新生成补丁，不能把两类变更合并成一个生产 PR。

## 已完成的只读验证

- `npm run governance:check`：通过。
- `demo-data-seed/scripts/test-demo-data-plan.mjs`：通过。
- `node --check demo-data-seed/scripts/seed-demo-data.mjs`：通过。
- 未执行造数、线上快照、生产迁移或部署。

## 下一步

1. 为 Personal Context 和 Security 分别生成仅包含对应文件/hunk 的可审查补丁清单。
2. 对 `demo-data-seed` worktree 的安全脚本做只读复审，确认是否作为唯一造数来源。
3. 记录素材归档位置和运维脚本安全审查结果，不把二进制直接带入业务 PR。
4. 完成后更新阶段索引、CLEAN-001 快照和本记录的验证结果；未完成前保持 C2“进行中”。

## 未验证项

- Personal Context 热补丁是否已在生产部署，尚未进行线上版本对账。
- 生产迁移 `20260808054219` 与仓库迁移 `20260808120000` 的对应关系尚未确认。
- 根目录各类文件尚未生成独立补丁或保全分支；本记录目前只是登记，不代表保全完成。
