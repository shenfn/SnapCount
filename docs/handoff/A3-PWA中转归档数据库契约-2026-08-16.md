# A3 PWA 中转归档数据库契约交接快照

> 任务：PWA-034 至 PWA-039
>
> 状态：实现完成，待远程 PostgreSQL 门禁
>
> 日期：2026-08-16
>
> 分支：`test/中转归档原子契约`
>
> 基线：`2430cf8`

## 已完成

- 建立原子归档 PostgreSQL 行为 fixture。
- 固定财务金额拒绝、账户流水、同域幂等、跨用户和 anon 隔离。
- 用故意失败的反馈触发器验证数据库事务回滚出口。
- 为不同域幂等重试新增最小域校验迁移，避免按新请求伪造目标引用。
- 将 fixture 和迁移双次执行接入 release validation。

## 冻结区

- 根工作区和其他 worktree 的全部 WIP。
- PWA/iOS 业务代码、Edge、生产数据、生产部署和迁移历史修复。

## 验证基线

- 本地治理、安全、重复识别契约、finance occurrence 和 PWA build 通过。
- 本机没有可用 PostgreSQL 服务；新增 SQL fixture 仍需 GitHub Actions 执行。

## 下一步

1. 提交、推送并创建本数据库契约 PR。
2. 远程 PostgreSQL fixture 全绿后合并。
3. 从最新 main 新建 `feature/PWA中转原子归档`，扩展现有 Staging Repository/Feature，删除客户端多步事务和 `0.01` 兜底。
