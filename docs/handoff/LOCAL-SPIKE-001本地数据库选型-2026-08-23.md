# LOCAL-SPIKE-001 本地数据库选型交接

> 更新时间：2026-08-23

## 当前状态

- 阶段：LOCAL-SPIKE-001，进行中。
- worktree：`D:\Business\count\.worktrees\LOCAL-SPIKE-001本地数据库选型`。
- 分支：`test/LOCAL-SPIKE-001本地数据库选型`。
- 基线：`origin/main@b864030`。
- 当前动作：隔离实验已建立，尚未取得 macOS/iOS Simulator CI 结果。

## 已完成

- 复核 iOS 17、Swift 5.9、XcodeGen、现有 Supabase Repository 和 XCTest 组织方式。
- 固定 LOCAL-SPIKE-001A-H 的选型场景与裁决规则。
- 建立独立 `LocalDatabaseSpikeHarness`，没有接入 `SnapCount` target。
- 为 GRDB 和 SwiftData 建立事务、回滚、唯一 ID、迁移和文件重启对比测试。
- 增加只针对 Spike 目录的 macOS/iOS Simulator workflow。

## 冻结范围

- 根工作区和其他 worktree WIP。
- `ios/SnapCount/**`、`ios/SnapCountTests/**`、`ios/project.yml`。
- PWA、Edge、Supabase、生产数据库、部署、TestFlight 和现有云端数据。

## 未验证

- 所有 Swift 编译和 XCTest，等待 GitHub macOS runner。
- SwiftData migration 与 unique 冲突的真实 runtime 行为。
- 真机 iOS Data Protection 和 SQLCipher 方案。

## 恢复入口

1. 读 `AGENTS.md`。
2. 读 `docs/spec/03-阶段与任务索引.md`。
3. 读 `docs/spec/模块/iOS本地优先/本地数据库选型Spike规格说明.md`。
4. 读 `docs/spec/执行记录/LOCAL-SPIKE-001本地数据库选型.md`。
5. 查看本分支 PR 和 `iOS Local Database Spike` workflow 日志。

## 下一步

推送并创建 PR；根据 macOS CI 获得真实证据后更新执行记录，随后编写数据库选型 ADR。没有 ADR 接受状态前，不进入 LOCAL-002 实现。
