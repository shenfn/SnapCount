# A4-IOS-006 iOS 截图还款候选与确认边界评估交接

更新时间：2026-08-21

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-006 iOS 截图还款候选与确认动作编排。
- 基线：`0449a369`（PR #128 合并提交）。
- 评估 worktree：`D:\Business\count\.worktrees\A4-IOS-006截图还款边界评估`。
- 分支：`docs/A4-IOS-006截图还款边界评估`。
- 本次范围：只读核对、边界评估、ADR 和交接；未修改 Swift、数据库、PWA、Edge 或 Planner。

## 已确认

1. iOS 现有 `Repository -> Use Case -> AppState -> View` 骨架继续作为目标架构，不需要重写。
2. `NativeRepaymentCandidateEngine` 已是纯函数，应继续保持快照输入和零 I/O。
3. `InboxRepository.confirmStagingRepayment` 已调用原子 RPC，是 transport 唯一入口。
4. 剩余问题是 AppState 的跨域编排：命令并发、用户/generation stale、accepted 后状态收敛和刷新失败仍需要结构化表达。
5. A4-IOS-005 已显式化周期准备，本片不得再次把 `ensure_liability_repayment_cycles` 混入候选确认。

## 下一步

1. 合并本评估 PR 后，从最新 `origin/main` 建立 A4-IOS-006 红灯 worktree。
2. 先写候选、Repository 源边界、确认 Use Case 并发/stale 和 accepted/refresh XCTest。
3. 最小实现 Use Case，再以兼容方式接回 `AppState.confirmStagingRepayment`；不改 RPC 和数据库。
4. 通过 macOS Build/XCTest、PWA/Edge 回归、治理与架构门禁后再收口。

## 未验证与剩余风险

- Windows 无 Swift/Xcode，本次未运行 iOS 编译或 XCTest。
- 未执行生产查询、migration、部署、真实账户写入或 TestFlight。
- 当前候选规则与 PWA-067 的行为含义已对照，但实现阶段仍需使用同一组脱敏场景验证排序和拒绝边界。
- 根工作区和其他 worktree 的 WIP 未触碰，禁止以本交接为由清理、重置或归类。
