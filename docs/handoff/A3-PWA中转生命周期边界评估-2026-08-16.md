# A3 PWA 中转生命周期边界评估交接快照

> 任务：PWA-026
>
> 状态：只读评估完成，待文档 PR 收口
>
> 日期：2026-08-16
>
> 分支：`docs/PWA中转生命周期边界评估`
>
> 基线：`39f4af8`

## 已完成

- PR #70 已合并，PWA-018 认证会话边界完成。
- 已完成 PWA 中转读取、重试、归档、丢弃、还款截图和待补全账单的只读复审。
- 已建立 PWA-026 评估、首片 Spec、ADR-017、执行记录。
- 已确认 REC-009 是下一片最小实现；归档跨表非原子差异单独阻断，不混入 REC-009。

## 当前结论

- PWA 中转边界目标为 `PagePending → useStore 门面 → Staging Feature → Staging Repository → Edge/RPC`。
- `retry_count` 与第四次拒绝由服务端权威；PWA 必须保留人工归档、修复和丢弃出口。
- `transactions.status = pending` 与 `staging_records` 统一展示但不统一状态机。

## 冻结区

- 根工作区及其他 worktree 的全部 WIP。
- 本评估分支不修改 PWA、Edge、数据库、iOS、部署、生产数据或历史中转记录。

## 验证基线

- `test:pending-queue`、安全契约、治理检查、架构 ratchet、`git diff --check`：通过。
- 未执行真实重试/归档/丢弃，也未进行生产历史孤儿审计。

## 未解决风险

- PWA 当前归档动作可能先成功写正式目标、后更新中转状态；需独立数据库/生命周期任务解决原子性。
- PWA 当前页面重试入口与 `retry_count` 上限的可见性需 REC-009 行为测试固定。
- `assigned/confirmed` 历史终态目标完整性未审计。

## 下一步

1. 逐文件暂存本评估文档并创建 PR。
2. PR 合并后从 `origin/main@39f4af8` 创建 `feature/PWA中转重试人工出口`。
3. 先红灯验证 REC-009，再实现最小 Repository/Feature；不修复归档原子性或历史数据。
