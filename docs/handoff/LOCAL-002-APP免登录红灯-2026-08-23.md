# LOCAL-002-APP 免登录接线红灯交接

## 当前状态

- 阶段：L1 / LOCAL-002。
- worktree：`D:\Business\count\.worktrees\LOCAL-002-APP免登录红灯`。
- 分支：`test/LOCAL-002-APP免登录红灯`。
- 基线：`6415f13`。
- 当前批次：LOCAL-002-APP 应用层基础和 AppState 未登录消费兼容入口绿灯；Root 免登录入口与本地账户准备尚未实现。

## 已完成

- `LocalExpenseAppUseCaseTests.swift` 固定 profile store、Use Case 和无云端依赖的编译入口。
- `test-ios-local-expense-app-boundary.mjs` 固定应用层文件、AppState 远端 session 前置和后续源边界。
- `npm run test:ios-local-expense-app-boundary` 已取得 2 项预期失败。
- 基础实现提交：`6cd173b`；当前同一边界测试已收敛为 1 项 AppState 接线失败。
- AppState 兼容接线提交：`77503c5`；编译修复：`db8ea2a`、`abb2e1b`。
- 行为补强提交：`fc71dec`；覆盖 profile 重启、Decimal 金额、真实 GRDB 事务、余额派生和本地读模型。
- 测试精度修复提交：`1917281`；仅将 SQLite 时间持久化比较调整为 1 毫秒容差，不改变生产语义。
- Run `32623778871`：完整 iOS XCTest `242/242`、模拟器 Build、iOS Build Gate 和全部适用门禁通过。
- `npm run test:ios-local-expense-app-boundary` 2/2 通过。
- `npm run governance:check` 通过。
- `npm run governance:arch` 通过，未增加架构基线违规。

## 冻结范围

- 根工作区和其他 worktree WIP。
- PWA、Edge、Supabase、生产数据库、发布配置和 TestFlight。
- 在红灯验证完成前，不修改现有 AppState、页面和远端 Repository 的业务实现。

## 下一步

1. 合并 PR #156；PR #155 的评估文档已被 #156 包含，不重复合并。
2. 从最新 main 新建独立 worktree，只读评估 Root 免登录入口与本地账户准备/选择出口。
3. 先定义未登录可见 Tab、功能降级和账户创建交互，再写 Spec/TDD；不得直接无条件放开全部 Tab。
4. 后续再接本地编辑、删除、详情和历史月份 UI；保持同步、AI 和其他域冻结。

## 未验证与风险

- Windows 无法运行 Swift 编译；最终证据来自 GitHub macOS CI。
- 默认本地账户交互、云端绑定、同步和其他域仍未实现。
- `RootView` 当前仍以 `isSignedIn` 决定 Login/Tab，尚无本地模式入口。
