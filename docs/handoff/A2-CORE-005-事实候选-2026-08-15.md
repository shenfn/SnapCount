# A2 CORE-005 事实候选交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心事实候选`
>
> 规格：`docs/spec/模块/表达核心/事实候选规格说明.md`
>
> 执行记录：`docs/spec/执行记录/CORE-005-事实候选TDD执行记录.md`

## 已完成

- CORE-005 已将支出当前记录、日聚合和实体首次出现候选迁入 `supabase/functions/_shared/expression-core/fact-candidates.mjs`。
- Edge Planner 已直接导入生产核心；Planner Lab 原路径保留为只 re-export 的兼容 wrapper。
- 共享 fixture 覆盖 CORE-024 至 CORE-029；独立 wrapper 测试覆盖 CORE-030。
- PR workflow 已加入事实候选 Node/Deno 双运行时检查。
- 架构 ratchet 的生产 `tools/` 依赖由 8 降至 7，旧事实候选导入已从例外清单移除。

## 当前边界

- `_shared/expression-core` 现在拥有数值/事实契约、表面选择/fallback、候选资格门、确定性评分和支出事实候选。
- 实体别名配置、recurrence、comparison、cross-record、generic-domain 和 render contract 仍留在 Planner Lab 基线。
- 不删除 `tools/ai-validation`，不修改 PWA、iOS、Prompt、迁移或生产部署。

## 本地验证

- CORE-005 Node 共享 fixture、Lab wrapper、旧事实候选 8 项回归以及前四片核心回归均已通过。
- 纯函数边界、治理入口和架构 ratchet 已通过。
- GitHub PR #49 的 Node/Deno、Edge、完整 Planner、治理、PWA 和迁移门禁均已通过，并已合并为 `c79ed56`。
- Windows 本地仍未安装 Deno，且缺少完整 Planner 回归所需的 `esbuild`；本地环境限制由 PR CI 覆盖。

## 下一步

1. 只读评估 recurrence，确认纯函数边界、调用点、既有测试和独立契约。
2. 评估通过后，新建 CORE-006 Spec、共享 fixture 和红灯；每片独立 PR，不能直接迁移多个候选模块。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
