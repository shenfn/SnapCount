# A4-IOS-003 iOS 账户还款动作编排红灯交接

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-003 iOS 手动账户还款确认/撤销动作编排 XCTest 红灯。
- 基线：`f4a889f`，PR #120 已合并。
- Worktree：`D:\Business\count\.worktrees\A4-IOS-003账户还款红灯`。
- 分支：`test/A4-IOS-003账户还款红灯`。

## 本次变更

- 新增 `ios/SnapCountTests/RepaymentActionUseCaseTests.swift`。
- 测试固定：无效输入/未登录拒绝、同命令复用、同账期冲突、accepted 与 refresh failure 分层、reset stale、撤销 canonical cycle。
- 未新增 `RepaymentActionUseCase` 实现；当前预期是红灯，不能把测试文件存在误认为功能完成。

## 验证状态

- Windows 无 Xcode/Swift 编译环境，未在本地运行 XCTest。
- `npm run governance:check` 与 `git diff --check`：红灯提交前通过。
- PR #121 首轮 macOS app build 阶段通过；XCTest 编译因缺少 `RepaymentActionUserContext`、`RepaymentActionUseCase` 和 `RepaymentActionResult` 失败，符合预期红灯，不是环境失败或测试语法失败。
- Run：`32264644828`，失败码 65，仅进入 Use Case 类型缺失错误。

## 下一步

1. 红灯已确认；在同一 PR 追加最小 `RepaymentActionUseCase` 与 AppState 兼容接线，保留红灯提交证据。
2. 运行源边界、治理、架构和 macOS 完整 XCTest/Build，确认由红转绿。
3. 保持 AccountRepository transport 兼容，不纳入截图还款、账户 CRUD、读取整体重构或数据库改动。
