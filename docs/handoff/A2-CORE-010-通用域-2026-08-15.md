# A2 CORE-010 通用域实现交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心通用域`
>
> Worktree：`D:\Business\count\.worktrees\表达核心通用域`

## 已完成

- PR #60 合并边界评估、Spec 和 ADR，基线为 `e19e792`。
- 建立 CORE-063 至 CORE-074 共享契约和 Node/Deno runner，并验证生产模块缺失时的有效红灯。
- 将通用域拆为稳定门面、共享基础、income、food、sleep 和 wallet 模块。
- Lab 原路径改为生产核心 wrapper，Edge Planner 改为生产门面 import。
- 新增 wrapper 同一性、Edge 静态契约和 CI Node/Deno job。
- 收紧架构依赖基线，生产仅剩 `render-contract.mjs` 一条 `tools` 反向依赖。

## 当前状态

- 当前阶段：A2 表达核心模块规格与特征测试。
- 当前任务：CORE-010 本地实现完成，等待提交、PR 和 CI。
- 已验证基线：`e19e792`。
- 当前提交：本交接随 CORE-010 实现提交一并落定。

## 验证结果

- CORE-010 Node fixture：1/1 通过。
- Planner Lab wrapper：1/1 通过。
- Edge 静态契约：1/1 通过。
- 旧通用域特征测试：27/27 通过。
- 全部表达核心 Node：9/9 通过。
- 完整 Planner：185/185 通过。
- PWA build、表达核心边界、架构 ratchet、治理检查和 diff 格式：通过。

## 未验证

- Windows 未安装 Deno，CORE-010 Deno runner 未在本地执行。
- Edge `deno check` 和完整 CI 尚未执行，必须以 PR workflow 为准。

## 剩余风险

- `render-contract.mjs` 仍由生产从 Planner Lab 导入，A2 尚未完成。
- `npm ci` 报告 1 个 moderate、4 个 high 的存量依赖风险；本切片没有升级依赖，不能用 `npm audit fix --force` 顺手扩大范围。
- 构建保留既有 `vconsole eval` 和 chunk 大小警告，不属于 CORE-010 行为失败。

## 冻结范围

- 根工作区和其他 worktree 的所有 WIP。
- PWA、iOS、数据库、生产配置、部署和 TestFlight。
- `render-contract` 规则，直到 CORE-011 Spec 建立。

## 下一步

1. 逐文件暂存并提交 CORE-010 实现。
2. 推送并创建 PR，等待全部 CI 门禁。
3. 合并后从最新 main 建立 CORE-011 `render-contract` 边界评估 worktree。

## 相关入口

- 模块 Spec：`docs/spec/模块/表达核心/通用域规格说明.md`
- ADR：`docs/decisions/ADR-012-通用域核心归属.md`
- 执行记录：`docs/spec/执行记录/CORE-010-通用域边界评估.md`
- 共享契约：`supabase/functions/_shared/contracts/表达核心-通用域-v0.1.json`
- 生产门面：`supabase/functions/_shared/expression-core/generic-domain-candidates.mjs`
