# A2 CORE-003 候选资格门禁交接快照

> 日期：2026-08-15
>
> 分支：`feature/表达核心资格门禁`
>
> 规格：`docs/spec/模块/表达核心/资格门禁规格说明.md`
>
> 执行记录：`docs/spec/执行记录/CORE-003-候选资格门禁TDD执行记录.md`

## 已完成

- CORE-003 已将候选硬门禁、商户活跃日实质性、surface 资格和周期路由迁入 `supabase/functions/_shared/expression-core/eligibility-gates.mjs`。
- Edge Planner 已直接导入生产核心；Planner Lab 原路径保留为只 re-export 的兼容 wrapper。
- 共享 fixture 覆盖 CORE-013 至 CORE-016；独立 wrapper 测试覆盖 CORE-017。
- PR workflow 已加入资格门 Node/Deno 双运行时检查。
- 架构 ratchet 的生产 `tools/` 依赖由 10 降至 9，旧资格门导入已从例外清单移除。

## 当前边界

- `_shared/expression-core` 现在拥有数值/事实契约、表面选择/fallback 和候选资格门。
- 候选生成、实体别名配置、确定性评分、比较、跨记录关系和 render contract 仍留在 Planner Lab 基线。
- 不删除 `tools/ai-validation`，不修改 PWA、iOS、Prompt、迁移或生产部署。

## 本地验证

- CORE-003 Node 共享 fixture、Lab wrapper、旧资格门 14 项回归、已有表达核心与计划选择回归均已通过。
- 纯函数边界、治理入口和架构 ratchet 已通过。
- Windows 未安装 Deno，且缺少完整 Planner 回归所需的 `esbuild`；GitHub CI 尚待运行。

## 下一步

1. 提交并创建 PR，验证 CORE-003 的 Node/Deno、Edge、完整 Planner、治理、PWA 和迁移门禁。
2. CI 全绿后，更新阶段索引为 CORE-003 已完成，并只读评估 `deterministic-scoring.mjs`；先建独立 Spec、fixture 和红灯，不能直接迁移。
3. A2 未完成前不开始 PWA/iOS 业务架构实现。
