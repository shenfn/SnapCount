# A3 PWA 账户管理实现收口交接快照

> 任务：PWA-066A 至 PWA-066F 实现收口
>
> 状态：已完成
>
> 基线：`34af7da`（PR #102 合并提交）
>
> 收口分支：`docs/PWA账户管理实现收口`

## 已完成

- PR #102 已合并，merge commit 为 `34af7da27a5f8e50d97f1247074de9d2c281ffbe`。
- 默认支出/收入账户互斥、归档默认清理、旧客户端直接写兼容和 canonical `save_account` / `set_account_archived` RPC 已落地。
- Account Repository、Account Management Feature、Store/Page 兼容接线、已归档账户分区与专用恢复出口已落地。
- PWA-066A 至 PWA-066F 的 Spec、ADR-027、执行记录、测试映射和完成定义均已有仓库内证据。

## 验证证据

- 本地账户管理专项 18 项、账户读取 19 项、还款 17 项、账户补绑 11 项和财务保存 16 项通过。
- `npm run build`、`npm run governance:check`、`npm run governance:arch` 和 `git diff --check` 通过；构建仅有既有 `vconsole eval` 与 bundle size 警告。
- PR #102 首轮 GitHub Actions run `31941675852` 全部适用门禁通过。
- PR #102 第二轮 Release Validation run `31941768740` 及全部适用门禁通过。
- PostgreSQL 17 job 连续执行 migration 两次，并验证默认互斥、旧客户端兼容、权限、归档恢复、跨用户拒绝、类型阻断、余额和历史不变量。

## 未验证与风险

- 本机未执行生产查询、生产 migration、部署、真实数据写入或 TestFlight。
- 生产存量账户是否存在迁移将规范化的数据形态仍未知，发布前必须另走生产迁移审批与核对。
- iOS 当前仍使用旧 REST 写入；数据库 trigger 已由 PostgreSQL 17 fixture 验证兼容，但 iOS transport 与 stale 语义仍属于后续 A4。

## 冻结范围

- 根工作区和其他 worktree 的 WIP。
- PWA-068 wallet 快照创建、关联、cycle 与余额校准。
- 流水 helper、iOS、Edge、Planner、生产配置、生产迁移与部署。

## 下一步

1. 从最新 `origin/main@34af7da` 建立 `docs/PWA截图还款边界评估`。
2. 只读评估 PWA-067：截图还款候选、`confirm_staging_repayment`、staging/accounts 协作事件、事务权威、幂等和失败出口。
3. 评估结论合并前不建立实现 Spec、不修改业务代码；PWA-068 继续独立冻结。
