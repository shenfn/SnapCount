# A2 CORE-010 通用域边界评估交接快照

> 日期：2026-08-15
>
> 分支：`docs/表达核心通用域边界评估`
>
> Worktree：`D:\Business\count\.worktrees\表达核心通用域边界评估`

## 已完成

- 读取当前 AGENTS、规格索引、阶段索引、TDD 规范和清洗总计划。
- 只读检查通用域源码、Planner 调用、现有特征测试和 `domainProfile` 输入。
- 确认通用域无 IO，公开入口稳定，但内部混合六个领域职责。
- 确认 wallet 需要独立安全规则和契约组。
- 建立 CORE-010 Spec、ADR 和执行记录。

## 当前状态

- 当前阶段：A2 表达核心模块规格与特征测试。
- 当前任务：CORE-010 通用域边界评估。
- 已验证基线：`f7b24cd`，工作区干净。
- 实现状态：未开始；当前仅有文档改动。

## 未验证

- Deno 双运行时尚未在 Windows 本地执行。
- 完整 Planner 回归是否能启动受本地 `esbuild` 依赖影响。
- 共享 fixture、生产核心模块和 Edge 新 import 尚未建立。

## 剩余风险

- 若实现阶段原样搬运单体文件，生产模块会继承领域混合和钱包规则耦合。
- `render-contract.mjs` 仍从 `tools` 被 Edge 引用，不能把 CORE-010 误报为 A2 全部完成。
- 食品 profile 保留 `n >= 5`；睡眠 chronotype 当前不检查 `n`。不得在迁移中顺手统一两者门槛。

## 冻结范围

- 根工作区和其他 worktree 的所有 WIP。
- PWA、iOS、数据库、Edge 查询/编排、生产配置和部署。
- `render-contract` 及其独立 CORE-011 评估。

## 下一步

1. 提交并推送本 docs 分支，创建文档 PR。
2. 文档 PR 合并后建立 `feature/表达核心通用域` 实现 worktree。
3. 按 CORE-063 至 CORE-076 建立共享 fixture 红灯并实施最小迁移。

## 相关入口

- 总规范：`docs/spec/00-系统总览规格说明.md`
- 阶段索引：`docs/spec/03-阶段与任务索引.md`
- 模块 Spec：`docs/spec/模块/表达核心/通用域规格说明.md`
- 边界评估：`docs/spec/模块/表达核心/通用域边界评估.md`
- ADR：`docs/decisions/ADR-012-通用域核心归属.md`
- 执行记录：`docs/spec/执行记录/CORE-010-通用域边界评估.md`
- 测试入口：`tools/ai-validation/expression-planner/tests/generic-domain-shadow-planner.test.mjs`
