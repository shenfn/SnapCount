# A3 PWA 表达计划边界实现交接快照

> 任务：PWA-001
>
> 状态：当前权威交接快照
>
> 日期：2026-08-16
>
> 分支：`feature/PWA表达计划边界`
>
> 基线：`60f40ef`（PR #65 合并提交）

## 已完成

- 先以目标模块缺失证明红灯，再新增 expression Repository 与纯 Feature State。
- `useStore` 保持 `recordExpressionPlanCache`、load、ack、submit 公开 API，只改为组合和转发。
- 用户切换 reset、同记录请求去重、revision/version 失效、交付身份校验、曝光绑定和 keepalive 反馈均有行为测试。
- 记录/中转/账户 mutation 仍只在服务端成功后失效表达缓存；24 个 Store 消费组件没有修改。
- 架构检查的陈旧 A2 提示已更新为 A3 目标，检查行为和 ratchet 数量未改变。

## 验证基线

```text
新 Feature/Repository + 既有 PWA 表达契约：22/22 通过
表达呈现规则：22/22 通过
finance occurred_at contract：通过
governance:check：通过
governance:arch：通过（0 / 5 / 0，无新增违规）
Vite production build：通过（153 modules transformed）
```

## 冻结范围

- 根工作区和其他 worktree 的全部 WIP。
- 认证、设置隐私、正式记录、中转生命周期、账户还款和数据域读模型边界。
- 页面视觉、组件交互、Edge、数据库、iOS、生产配置、部署与 TestFlight。

## 未验证与风险

- GitHub PR 门禁尚未运行；合并前 PWA-001 不标记为最终完成。
- `npm ci` 报告 5 个既有依赖漏洞；Vite 报告 vconsole `eval` 与大 chunk 警告，均未在本切片扩展处理。
- 页面 Supabase 直连基线仍为 5，PWA-001 没有预先宣告清零。
- 旧源码契约已改为“门面/失效顺序”与“Feature 内部边界”分层检查；真实行为由新增 Node 测试承担。

## 下一步

1. 逐文件检查差异并提交本分支。
2. 获得用户授权后推送并创建 PR，等待全部适用门禁。
3. PR 合并后更新 PWA-001 为完成，并从最新 main 建立“设置与隐私”只读评估切片。
4. PWA-001 合并前不开始下一业务边界实现，不清理任何历史 worktree。
