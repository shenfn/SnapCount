# A4-IOS-003 iOS 账户还款动作编排评估交接

## 当前状态

- 阶段：A4 iOS 业务编排收拢。
- 任务：A4-IOS-003 iOS 手动账户还款确认/撤销动作编排边界评估。
- 基线：`2f6b389`，A4-IOS-002 收口 PR #119 已合并。
- Worktree：`D:\Business\count\.worktrees\A4-IOS-003业务边界评估`。
- 分支：`docs/A4-IOS-003业务边界评估`。
- 本次变更：只新增评估 Spec、ADR、阶段索引和本交接；未修改 AppState、Repository、数据库、PWA、Edge、Planner 或生产配置。

## 已完成

1. 复核 `AppState.confirmRepayment`、`AppState.revokeLiabilityPayment` 和 `refreshAfterRepayment` 的现有职责。
2. 确认 `AccountRepository` 已封装两个 canonical RPC：`set_repayment_cycle_paid_amount` 与 `revoke_liability_payment`。
3. 选择手动确认/撤销作为 A4-IOS-003 最小切片，排除截图还款、账户 CRUD、读取整体重构和数据库改动。
4. 固定 accepted、refresh failure、generation/user stale、同命令复用和冲突行为契约。

## 验证

- `git diff --check`：待本 worktree 文档修改完成后执行。
- `npm run governance:check`：待本 worktree 文档修改完成后执行。
- Windows 无 Swift/Xcode；未执行 XCTest，后续以 GitHub macOS Build/XCTest 为准。

## 未决项与风险

- `AccountRepository.confirmRepayment` 当前在 Repository 内生成 `paid_at`；实现 Spec 需裁决是否注入时钟。
- 账户详情刷新仍是分区读取，不能在本片顺手改成整体读取 Use Case。
- 还款 RPC 和数据库余额事务不在本片修改范围。

## 下一步

1. 合并本评估 PR。
2. 从合并后的 main 建立 A4-IOS-003 XCTest 红灯 worktree。
3. 先写 Use Case 行为测试，再做最小实现和 AppState 兼容接线。
4. 以 macOS GitHub Actions 的专项 XCTest、完整 Build、治理和架构门禁作为实现完成依据。
