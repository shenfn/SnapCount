# A2 CORE-011 渲染契约边界评估交接快照

> 状态：历史快照；当前状态以 `docs/handoff/A2阶段收口-2026-08-15.md` 为准。
>
> 日期：2026-08-15
>
> 分支：`docs/表达核心渲染契约边界评估`
>
> Worktree：`D:\Business\count\.worktrees\表达核心渲染契约边界评估`

## 已完成

- 确认 CORE-010 PR #61 全绿并合并到 `082f0db`。
- 读取当前 AGENTS、阶段索引、总计划和 A2 规格链。
- 只读检查 render contract、旧测试、Edge Planner/Delivery/Shadow 和架构基线。
- 确认模块适合整体迁移，版本常量必须成为运行时单一事实源。
- 建立 CORE-011 边界评估、Spec、ADR 和执行记录。

## 当前状态

- 当前阶段：A2 表达核心模块规格与特征测试。
- 当前任务：CORE-011 渲染契约边界评估。
- 已验证基线：`082f0db`，工作区开工前干净。
- 实现状态：未开始；当前仅有文档改动。

## 未验证

- CORE-011 共享 fixture 和生产模块尚未建立。
- Deno/Edge 双运行时需由实现 PR CI 验证。
- A2 最终 fresh-agent 接续和阶段完成证据尚未执行。

## 剩余风险

- 版本值在三个运行时文件重复，迁移只改 Planner import 而不收敛常量会留下静默漂移。
- Lab exposure helper 不得被误接入生产持久化。
- 默认当前时钟必须保留兼容，但共享 fixture 必须显式传时间。

## 冻结范围

- 根工作区和其他 worktree 的所有 WIP。
- PWA、iOS、数据库迁移、生产配置、部署和 TestFlight。
- Edge 查询、持久化和交付编排；实现仅允许 import/常量改线。

## 下一步

1. 逐文件提交并推送本 docs 分支，创建文档 PR。
2. 文档 PR 合并后建立 `feature/表达核心渲染契约`。
3. 按 CORE-077 至 CORE-084 完成 fixture、最小迁移和 PR CI。

## 相关入口

- 模块 Spec：`docs/spec/模块/表达核心/渲染契约规格说明.md`
- 边界评估：`docs/spec/模块/表达核心/渲染契约边界评估.md`
- ADR：`docs/decisions/ADR-013-渲染与曝光契约归属.md`
- 执行记录：`docs/spec/执行记录/CORE-011-渲染契约边界评估.md`
- 旧测试：`tools/ai-validation/expression-planner/tests/render-contract.test.mjs`
