# ═══════════════════════════════════════════════════════════════
# SnapCount 前端环境变量示例
# 复制此文件为 .env.local 后填入真实值（.env.local 已在 .gitignore 中）
# ═══════════════════════════════════════════════════════════════

# Supabase 项目地址
# 方式一：直连 Supabase 原始域名
# VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
#
# 方式二：通过 Cloudflare Worker 反代（推荐，可自定义域名 + 控制 CORS）
VITE_SUPABASE_URL=https://<your-worker-domain>

# Supabase anon（public）key
# 位置：Supabase Dashboard → Project Settings → API → Project API keys → `anon` `public`
# 此 key 设计上就是公开的，会随前端代码发给所有用户；真正的权限由 RLS 策略控制
VITE_SUPABASE_ANON_KEY=<your-supabase-anon-key>

# ═══════════════════════════════════════════════════════════════
# 以下为 Edge Function / Cloudflare Worker 所需的后端 secret（不放前端）
# 仅作说明，不要填在此文件中。部署时通过各平台的 Secret 管理注入：
# ═══════════════════════════════════════════════════════════════
#
# Supabase Edge Function secrets（通过 `supabase secrets set` 设置）：
#   SUPABASE_URL              - 同上
#   SUPABASE_SERVICE_ROLE_KEY - Supabase Dashboard → API → service_role（严禁泄露）
#   MOONSHOT_API_KEY          - Moonshot / Kimi Vision API Key（sk-xxxx）
#   RELAY_API_KEY             - 自建 OpenAI 兼容中转站 API Key
#   RELAY_ENDPOINT            - 可选，默认 http://47.76.157.150:8317/v1/chat/completions
#   RELAY_MODEL               - 可选，默认 gpt-5.4
#
# Cloudflare Worker 环境变量（在 Workers Settings → Variables 设置）：
#   SUPABASE_URL              - 同上
#   SUPABASE_ANON_KEY         - 同上
